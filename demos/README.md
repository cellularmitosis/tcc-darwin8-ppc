# Demos

One runnable demo per milestone, showcasing the new capability. Each
is a tiny C program that compiles and runs end-to-end on a real G3
running Tiger 10.4.11 via `./tcc -B. -run demos/<file>.c`.

The naming scheme is `sNNN-<slug>.c` for sessions before the first
versioned release. Once we ship `v0.1.0-g3` (the first /opt-installable
tarball), demos for new capabilities use `v<X>.<Y>.<Z>-<slug>.c`.

| Demo | Milestone | Expected exit | What it proves |
|---|---|---|---|
| [s005-return-the-answer.c](s005-return-the-answer.c) | [005 — `tcc -run` on G3](../docs/sessions/005-macho-stubs/README.md) | `42` | First end-to-end JIT execution of tcc-generated PPC code. Function prologue/epilogue, immediate load, blr return. |
| [s006-factorial.c](s006-factorial.c) | [006 — locals + arith + control](../docs/sessions/006-locals-arith-control/README.md) | `120` | Locals, integer arithmetic, while-loop, conditional branch — the first program with real computation. |
| [s007-fibonacci.c](s007-fibonacci.c) | [007 — function calls (Apple PPC ABI)](../docs/sessions/007-function-calls/README.md) | `55` | Recursive `fib(10)`. Caller-side arg packing in r3-r10, callee-side parameter spill, `bl` with R_PPC_REL24 relocation patched at JIT time, frame preservation across nested recursion. |
| [s008-array-sum.c](s008-array-sum.c) | [008 — pointers + modulo](../docs/sessions/008-pointers-modulo/README.md) | `55` | Sums an `int[10]` filled with 1..10 via a pointer-passing helper, returns `sum % 256`. Pointer deref, store-via-pointer, array-to-pointer decay, modulo. |
| [s009-real-macho.sh](s009-real-macho.sh) | [009 — real PPC Mach-O .o](../docs/sessions/009-tccmacho-ppc/SESSION_README.md) | `42` | tcc emits a real PPC Mach-O `.o`, gcc-4.0 links it, the executable runs on G3. First on-disk linkable output. |
| [s010-long-long.c](s010-long-long.c) | [010 — long long codegen](../docs/sessions/010-long-long/README.md) | `120` | 64-bit add through a function: `add64(0x12345678LL, 0x10000000LL)` low byte. Long long params (r3:r4 + r5:r6), returns, addc/adde. |
| [s011-struct.c](s011-struct.c) | [011 — structs](../docs/sessions/011-structs/README.md) | `42` | Two struct types, member access (`.` and `->`), pass-by-pointer to helpers, struct holding pointer-to-array. Confirms structs work for the patterns tcc itself uses. |
| [s012-varargs.c](s012-varargs.c) | [012 — varargs](../docs/sessions/012-varargs/README.md) | `42` | `sum_n(7, 1..7) + 14 = 42`. Standard `<stdarg.h>`. Prolog now spills all 8 GPR arg slots to caller's parameter save area so the standard char*-based va_list machinery walks forward correctly. |
| [s013-floating-point.c](s013-floating-point.c) | [013 — IEEE 754 single + double FP](../docs/sessions/013-floating-point/README.md) | `27` | `poly(a, b, c, x) = a*x*x + b*x + c` evaluated as `poly(1, 2, 3, 4)`. FP load/store/arithmetic, FP arg passing (f1..f8), int↔FP conversions, FP comparisons. |
| [s016-hello-printf.sh](s016-hello-printf.sh) | [016 — PPC PIC stubs](../docs/sessions/016-ppc-plt-stubs/README.md) | prints `hello from tcc-built program` | First-ever `printf` from tcc-emitted code on G3. PowerPC `__picsymbolstub1` + `__la_symbol_ptr` + indirect symbol table for late-binding external function calls. |
| [s025-self-link.sh](s025-self-link.sh) | [025 — Mach-O .o reader](../docs/sessions/025-macho-o-reader/README.md) | prints `hello from tcc-built and tcc-linked program` | Full self-link via tcc, no gcc. `tcc -o exe file.c` auto-loads `/usr/lib/crt1.o` and produces a working printf+malloc+strcpy executable end-to-end. |
| [v0.2.12-lua.sh](v0.2.12-lua.sh) | [040 — sqlite3/lua pickup](../docs/sessions/040-pickup-2026-05-03/README.md) | prints `fib(20) = 6765` then `done` | First non-trivial third-party program: lua 5.4.7 (~30 .c files, full standard library) builds and runs end-to-end with tcc. Surfaced two backend bugs (struct-deref-by-value; cross-TU PIC reloc translation) that v0.2.12 fixes. |
| [v0.2.17-alloca.sh](v0.2.17-alloca.sh) | [043 — unsupervised](../docs/sessions/043-unsupervised-2026-05-05/README.md) | prints `concatenated: jack/jill/jane` then `done` | `alloca()` actually works. Pre-v0.2.17, allocating with alloca and then doing anything else (printf, strcpy) silently corrupted the alloca'd memory and SEGV'd on epilog. Two coupled bugs: function epilog used `addi r1, r1, frame_size` (broken when alloca moves r1) and alloca's safety zone was 32 bytes (too small to absorb subsequent param spills). New epilog uses back-chain + FP restore; new alloca reserves 256 bytes. |
| [v0.2.18-bzip2.sh](v0.2.18-bzip2.sh) | [043 — unsupervised](../docs/sessions/043-unsupervised-2026-05-05/README.md) | prints two identical MD5 sums, then `PASS` | bzip2 1.0.8 builds and round-trips a 573KB file (`/etc/services`) end-to-end with tcc. Compress + decompress produces a byte-identical copy. Third real-world program (after lua and zlib) — bzip2 exercises a different mix of codegen paths (heavy bitwise, large lookup tables, BWT, Huffman). Surfaced and fixed two unrelated codegen bugs in the run-up: variadic-FP-past-8-FPRs off-by-one (v0.2.18) and float negation on big-endian (v0.2.18). |
| [v0.2.21-cjson.sh](v0.2.21-cjson.sh) | [043 — unsupervised](../docs/sessions/043-unsupervised-2026-05-05/README.md) | prints `name=alice age=30` then `sum=15` | cJSON 1.7.18 builds and parses a JSON object end-to-end with tcc. Fourth real-world program (after lua, zlib, bzip2). Exercises a different mix of paths: heavy linked-list traversal, strdup/free patterns, recursive descent parsing. Surfaced one minor missing-symbol gap (v0.2.21 added `__builtin_fabs` / `__builtin_inf` stubs because Tiger's `<math.h>` macros expand to those). |
| [v0.2.21-httpd.c](v0.2.21-httpd.c) | [043 — unsupervised](../docs/sessions/043-unsupervised-2026-05-05/README.md) | run `./httpd 8080 &` then `curl http://127.0.0.1:8080/` returns `hello from tcc-built httpd` | First interactive network program built with tcc-darwin8-ppc. Exercises BSD sockets via libSystem (socket/bind/listen/accept/recv/send/setsockopt/htons/htonl). All worked first try — proves the BSD-socket-via-libSystem path has no extra codegen surprises beyond what cJSON / bzip2 / lua / zlib already exposed. |
| [v0.2.22-relocation-addends.c](v0.2.22-relocation-addends.c) | [044 — unsupervised](../docs/sessions/044-unsupervised-2026-05-05/README.md) | prints `*rel1 = 200`, `*rel2 = 300`, `*tp.a = 200`, `*tp.b = 300` and exits 0 | `int *p = &arr[N]` (N != 0) static initializers actually point at `&arr[N]`. Pre-v0.2.22, all such initializers collapsed to `&arr[0]` — the PPC `R_PPC_ADDR32` relocator OVERWROTE the in-place value (which is the implicit ELF Rel addend per i386 convention) instead of ADDING to it. Same OVERWRITE-not-ADD bug also lived in the Mach-O exe writer; .o-roundtrip needed an extra fix in the .o loader to zero the in-place after extracting the addend into a synthetic anchor sym. Drops `tcctest.c` diff vs gcc reference from 44 → 33 lines. |
| [v0.2.23-sqlite-file.sh](v0.2.23-sqlite-file.sh) | [044 — unsupervised](../docs/sessions/044-unsupervised-2026-05-05/README.md) | creates `/tmp/v0223-sqlite-demo.db`, populates a 3-row table, prints rows ordered by age, exits 0 | **Full sqlite3 with on-disk database — fifth real-world program.** Pre-v0.2.23, `sqlite3_open("file.db", ...)` SEGV'd inside `unixOpen → osFcntl(...)`. The data slot for `(sqlite3_syscall_ptr)fcntl` (a static initializer in sqlite's `aSyscall[]` table) held the `__nl_symbol_ptr` slot ADDRESS, not the function address — so the indirect call jumped into 4 bytes of data and faulted. Fix: when an extern function is data-referenced via `R_PPC_ADDR32`, allocate a stub for it (in addition to the existing trigger of `R_PPC_REL24` calls), and store the stub's address in the slot. Calling through the stub correctly reaches the real libSystem function. Closes the deferred sqlite file-open SEGV from session 041, plus (because the `&arr[N]` v0.2.22 fix landed first) the deferred CREATE TABLE → SQLITE_CORRUPT bug from the same session. |
| [v0.2.25-dylib.sh](v0.2.25-dylib.sh) | [045 — unsupervised dylib](../docs/sessions/045-unsupervised-dylib-2026-05-06/README.md) | builds `libgreet.dylib`, dlopens it, prints three greetings, exits 0 | **First Mach-O dylib output.** `tcc -shared -o libfoo.dylib foo.c` produces a real PPC Mach-O dynamic library that dyld can load via `dlopen`. New `macho_output_dylib()` in `ppc-macho.c`: MH_DYLIB filetype, no `__PAGEZERO`, no `LC_UNIXTHREAD`, `LC_ID_DYLIB` instead, vmaddr defaulting to 0x40000000. Function-call stubs are PIC (8 instructions / 32 bytes per stub) so they survive dyld's slide. `LC_DYSYMTAB` always emitted (even with all-zero counts) — without it, dyld faults in `doBindExternalRelocations` during dlopen. Runs constructors via `__mod_init_func`, supports static data, libSystem calls (printf/strlen), pthread mutex, long-long arithmetic — all verified end-to-end. |
| [v0.2.26-link-dylib.sh](v0.2.26-link-dylib.sh) | [045 — unsupervised dylib](../docs/sessions/045-unsupervised-dylib-2026-05-06/README.md) | prints `zlib version: 1.2.3` and adler32 hex, otool shows libz LC_LOAD_DYLIB | **Link-time dylib support:** `tcc -lz hello.c` actually works at runtime. `macho_load_dll()` (was a no-op) now parses the dylib's `LC_SYMTAB`, registers each defined-external as UNDEF in our symtab, captures the install name from `LC_ID_DYLIB`, and adds a `DLLReference`. The exe writer emits one `LC_LOAD_DYLIB` per loaded dll. With extra dylibs loaded, the writer switches to FLAT namespace (clears `MH_TWOLEVEL`) so dyld searches all loaded dylibs without per-symbol ordinal tracking. **Closes upstream `dlltest`** — tcc compiles libtcc.c into libtcc2.dylib, links tcc2 against it, runs ex1.c through the round trip. |
| [v0.2.27-jit-heisenbug.sh](v0.2.27-jit-heisenbug.sh) | [045 — unsupervised dylib](../docs/sessions/045-unsupervised-dylib-2026-05-06/README.md) | 30 iterations of JIT-compile-and-run a struct-returning function; prints `30 iterations OK` | **JIT heisenbug fixed.** Pre-v0.2.27, this loop crashed with SIGILL/SIGBUS/SIGSEGV (or returned wrong values) at a random iteration. Root cause: when tcc compiles itself, `__TINYC__` is set and the inline-asm `__clear_cache` falls through to a no-op stub — the JIT path never actually flushed the icache. JIT page reuse across iterations saw stale instructions. Fix: delegate to libSystem's `sys_icache_invalidate` (one-line change). Closes a 5+-session deferred issue blocking abitest-tcc, libtest_mt, test3 and dlltest stability. |
| [v0.2.28-clean-abitest.sh](v0.2.28-clean-abitest.sh) | [045 — unsupervised dylib](../docs/sessions/045-unsupervised-dylib-2026-05-06/README.md) | abitest-tcc completes 24/24 sub-tests; abitest-cc shows the known long-double diffs | **abitest-tcc 24/24 + PPC backtraces.** Two fixes: (a) `gfunc_call` switched to `save_regs(nb_args)` so the func-ptr LVAL's volatile-GPR address survives arg-setup clobber. Closes `many_struct_test_3` Bus error and lets `stdarg_*` and `arg_align_test` pass. (b) `rt_get_caller_pc` + `rt_getcontext` Tiger PPC (`mcontext->ss.{srr0,r1}`). libtest_mt "producing some exceptions" now passes; test1b prints real backtrace addresses; signal-handler longjmp path finally works on PPC. |
| [v0.2.28-sqlite-dylib.sh](v0.2.28-sqlite-dylib.sh) | [045 — unsupervised dylib](../docs/sessions/045-unsupervised-dylib-2026-05-06/README.md) | builds `libsqlite3.dylib` (~1.9MB), dlopens it, prints `sqlite3 version: 3.46.1` then `sqlite3 dylib end-to-end OK` | **Real-world dylib stress test.** Builds the entire 250-kloc sqlite3 amalgamation as a tcc-produced dylib — exercises a wide swath of codegen against a non-trivial codebase. The dlopen test exe finds `sqlite3_libversion`, `sqlite3_open`, `sqlite3_exec`, `sqlite3_close` via dlsym and runs CREATE TABLE + INSERT in `:memory:` to confirm the dylib really works end-to-end, not just structurally. |
| [v0.2.29-dylib-slide.sh](v0.2.29-dylib-slide.sh) | [046 — dylib slide](../docs/sessions/046-dylib-slide-2026-05-06/README.md) | dylib loaded both at preferred vmaddr and after `mmap(MAP_FIXED)` blocker forces a slide; both runs exercise pointer-to-data, pointer-to-pointer, fn-pointer, fn-pointer-array, constructor; both print `PASS` | **Dylib slide support.** Pre-v0.2.29, tcc dylibs only worked at the preferred vmaddr (0x40000000); if dyld slid them, absolute references in `__data` and absolute `lis/addi` pairs in `__text` pointed at unslid VAs. Two changes: (a) `ppc_need_pic_for_sym` now returns 1 for locally-defined symbols in DLL mode, routing code refs through the PIC sectdiff (sym − anchor) pattern that's invariant under slide; PIC base setup pinned to function prolog so it always runs before any subsequent PIC use, regardless of control flow. (b) `macho_output_dylib` walks the reloc tables of __data / __mod_init_func / __mod_term_func and emits a Mach-O VANILLA local `relocation_info` into LC_DYSYMTAB.locrel for every R_PPC_ADDR32 against a locally-defined symbol; dyld walks this table at load time and adds the slide amount to each location, fixing static-init pointers like `int *p = &arr[3]` and arrays of function pointers. |
| [v0.2.31-libtcc-slide.sh](v0.2.31-libtcc-slide.sh) | [046 — dylib slide](../docs/sessions/046-dylib-slide-2026-05-06/README.md) | builds a dylib with a 5-entry `static const struct { const char *name; int value; } tbl[]` table backing a `lookup(name)` function; runs lookups both at preferred vmaddr and after `mmap(MAP_FIXED)` blocker forces slide; both runs print `all 6 lookups ok`; final `PASS` | **Const-data-with-pointers slide fix.** v0.2.29's locrel emission covered `__data`/`__mod_init_func`/`__mod_term_func` but not `__const`. Pointer-bearing `static const struct {...} tbl[]` tables landed in `__TEXT,__const` (read-only); dyld couldn't slide the embedded `name` pointers and any dereference crashed under slide. Fix: in `tccgen.c`, when `output_type == TCC_OUTPUT_DLL`, route initialized const data to `data_section` instead of `rodata_section`. Trade-off: lose the RO protection for const data in dylibs, but ensure correctness under slide. With this, libtcc.c compiled as `-shared` produces a libtcc2.dylib whose `tcc_options[]`-style tables now survive slide; `tcc_compile_string` + `tcc_relocate` + `tcc_get_symbol` all work end-to-end on a slid libtcc2 (pre-v0.2.31 SIGBUS'd in `tcc_compile_string`). |
| [v0.2.30-alacarte.sh](v0.2.30-alacarte.sh) | [046 — dylib slide](../docs/sessions/046-dylib-slide-2026-05-06/README.md) | builds a 3-member archive (`lib_a.o`, `lib_b.o`, `lib_c.o`); links a program that references only `lib_a` and `lib_b`; verifies the resulting binary contains those two and NOT `lib_c`; prints `PASS` | **Mach-O archive alacarte loader.** Pre-v0.2.30, tcc force-loaded every member of every `.a` archive on Mach-O (whole-archive). v0.2.30 parses the BSD `__.SYMDEF SORTED` (or `__.SYMDEF`) symbol table at the start of the archive, mapping each defined symbol to its member's file offset; at link time tcc walks its undefined-symbol set and pulls in only the members that resolve a reference, iterating until no new members get added (so transitive dependencies work). Also extends the SysV-style `/` symdef path (used by `tcc -ar`-built archives) to sniff each loaded member's magic and route Mach-O `.o` files through `macho_load_object_file`, so libtcc1.a keeps loading correctly while gaining alacarte semantics. Closes structural roadmap item #4. |
| [v0.2.37-dwarf-obj.sh](v0.2.37-dwarf-obj.sh) | [052 — DWARF in .o output](../docs/sessions/052-dwarf-obj-2026-05-07/README.md) | compiles a small program with `tcc -c -gdwarf-2`; verifies the .o has a `__DWARF` segment with `__debug_*` sections; runs `dwarfdump` and confirms it parses the compile unit + types + producer string | **DWARF debug info in `-c` Mach-O output.** Pre-fix, `tcc -c -g` accepted `-g` but emitted no DWARF. v0.2.37 wires up tccdbg.c for PPC (CIE: return-addr=65/LR, SP=r1, code-align=4, data-align=-4; instr-len=4; target-endian writes since DWARF follows BE on PPC32); adds R_PPC_REL32 to ppc-link.c; teaches ppc-macho.c's classify_section to map `.debug_*` to `__DWARF,__debug_*` with S_ATTR_DEBUG and addr=0 (so cross-section R_PPC_ADDR32 relocs resolve to "0+addend=addend"). DWARF in the linked exe path is the natural follow-up. |
| [v0.2.38-dwarf-exe.sh](v0.2.38-dwarf-exe.sh) | [053 — DWARF in linked exe](../docs/sessions/053-dwarf-exe-2026-05-08/README.md) | compiles + links a small program with `tcc -gdwarf-2`; verifies the linked exe has a `__DWARF` segment with `__debug_*` sections; runs the exe (sq(5)=25); confirms `dwarfdump` reads compile unit + finds `sq` subprogram + `AT_low_pc` matches the actual runtime VA from `nm`; verifies line table has address ranges | **DWARF debug info in linked Mach-O exe.** Closes the second half of the DWARF arc started in v0.2.37. `macho_output_exe` walks `s1->sections[]` for `S_ATTR_DEBUG` sections and lays them into a `__DWARF` segment placed after `__LINKEDIT` (vmaddr=0/vmsize=0; real file_offset/file_size; per-section headers carry S_ATTR_DEBUG). DWARF cross-section R_PPC_ADDR32 resolves to "0+addend=addend" (debug section vmaddr=0); DWARF→.text R_PPC_ADDR32 resolves to text_runtime_va + addend. Adds R_PPC_REL32 case to `exe_resolve_section_relocs` (used by `.eh_frame` FDEs) and fixes the matching addend-loss bug in ppc-link.c::relocate. dwarfdump reads compile unit / abbrev / line table / aranges / strings; AT_low_pc / AT_high_pc carry the actual runtime VAs. (Tiger's `gdb 6.3` doesn't read embedded `__DWARF` segments — it expects OSO STAB references or a `.dSYM` bundle — so step-debugging needs follow-up. lldb on later releases reads embedded DWARF natively.) |
| [v0.2.39-eh-frame.sh](v0.2.39-eh-frame.sh) | [053 — DWARF in linked exe](../docs/sessions/053-dwarf-exe-2026-05-08/README.md) | links a 3-function program with bare `-g`; verifies `__TEXT,__eh_frame` is present in the exe; checks dwarfdump --eh-frame reads CIE (ra_register=0x41, CAF=4, DAF=-4) + 5 FDEs with per-prolog CFI (advance_loc 96; def_cfa_offset frame_size; offset_extended 65, -(frame_size-8)); confirms default DWARF version is now 2 (was 4 pre-v0.2.39); runs the exe end-to-end | **DWARF `__eh_frame` + per-prolog CFI in linked Mach-O exe.** Three coupled changes: (a) tcc.h enables `TCC_EH_FRAME` for `TCC_TARGET_MACHO` (was excluded by `ELF_OBJ_ONLY`); tccelf.c keeps `unwind_tables` on for non-ELF outputs when `-g`; classify_section maps `.eh_frame` → `__TEXT,__eh_frame`; `macho_output_exe` discovers + lays out + relocates + emits eh_frame. (b) tccdbg.c's PPC FDE block emits `DW_CFA_advance_loc 96; DW_CFA_def_cfa_offset frame_size; DW_CFA_offset_extended 65, (frame_size-8)/4` per FDE — describing the CFA shift across the prolog's `stwu` and the LR save. ppc-gen.c's gfunc_epilog stashes frame_size in `ppc_last_frame_size` for tccdbg.c to read. (c) Mid-section .eh_frame terminator strip in macho_output_exe: each input .o that tcc compiled with unwind_tables on contributes a trailing 4-byte zero record; the merged exe-side .eh_frame had multiple internal terminators which made dwarfdump stop at the first one (and Bus-error on bytes past it). The cleanup walks records, drops length=0, fixes up FDE CIE_pointers, re-appends a single clean terminator. Bonus: configure now defaults `-g` to DWARF-2 on Darwin (was 4) — Tiger's bundled tools fully understand DWARF-2 and only partially DWARF-4. |
| [v0.2.40-sed.sh](v0.2.40-sed.sh) | [054 — unsupervised](../docs/sessions/054-unsupervised-2026-05-09/README.md) | downloads + builds + runs GNU sed 4.8 on Tiger; confirms Makefile uses `CC=tcc` and the linked binary references only `libSystem.B.dylib` (no `libgcc_s`); runs 7 functional checks across BRE / ERE / address ranges / backrefs / intervals / line delete / append; prints `PASS` | **GNU sed 4.8 builds and works end-to-end with tcc** — sixth real-world program (after lua, zlib, bzip2, cJSON, sqlite). Three substantive tcc fixes had to land in v0.2.40: (1) `_Bool *p; *p = b;` (VT_BOOL store via pointer) was unhandled in `ppc-gen.c::store()` and load-via-sym+off; (2) `__attribute__((gnu_inline))` was unrecognized — gnulib's `c-ctype.h` uses `_GL_INLINE` which on gcc-4.x defaults to gnu89-inline semantics; without the attribute support every TU emitting c-ctype.h's inlines tripped "defined twice" link errors; predefining `__GNUC_GNU_INLINE__=1` on Apple steers gnulib to the attribute path tcc now honours. (3) PIC SECTDIFF reloc translation: when tcc parses `regex.c` it sees `extern reg_syntax_t re_syntax_options;` from `regex.h` first (PIC reloc emitted), then a tentative def of the same symbol later in the same TU. The .o writer baked the .o-internal SECTDIFF displacement into the addis/addi immediates; the loader was silently dropping the reloc translation when its target lived in a non-discarded merged section (BSS here), leaving the wrong constant in the linked exe. Result: `re_compile_pattern` read 0 for the syntax flag, ERE `(...)` and BRE `\{n,m\}` patterns silently failed to compile. Fix: in `macho_translate_relocs`, when a SECTDIFF target is a real merged section, synthesize an anonymous local anchor sym at the target offset and emit R_PPC_*_PIC against it — the existing PIC-fallback resolver handles the rest. |
| [v0.2.50-self-link-diagnostics.sh](v0.2.50-self-link-diagnostics.sh) | [068 — self-link diagnostics](../docs/sessions/068-self-link-diagnostics-2026-05-12/README.md) | compiles a small program touching every emitted section class (text / rodata / data / bss / libSystem stubs / function-pointer init) end-to-end and prints `rodata works \| data=42 \| bss=0 33 77 \| p=ok`; the diagnostics fire only on regressions so the happy path proves they're non-disruptive | **Pre-write sanity checks in `macho_output_exe` catch the broken-image classes that historically only surfaced as cryptic dyld errors at run time** (`Cannot allocate memory`, `Symbol not found: __mh_execute_header`, `SIGBUS during crt1 startup`). Four invariants are now checked before a single byte is written: (a) VM layout — segments page-aligned, no overlap, `vmsize >= filesize`, `__LINKEDIT` placed after `__TEXT+__DATA` in both VA and file space (catches the session-025 "Cannot allocate memory" dyld failure); (b) `__mh_execute_header` registered as `N_ABS` at `__TEXT` base + `entry_addr` inside `__text`; (c) every stub VA inside `__symbol_stub1`, every `__nl_symbol_ptr` slot inside `__nl_symbol_ptr`, every `ST_PPC_NEEDS_STUB` symbol has a stub allocated (catches the session-025 crt1 SIGBUS); (d) every defined symbol's section is in the EXE writer's emitted section list (catches the "common symbol in `.bss` but `__bss` not emitted" case). Each invariant was hand-verified by injecting a deliberate break and observing the resulting tcc-level error — see the session 068 README for the procedure. Costs <1ms per link; no behavior change on the happy path. |
| [v0.2.57-strip-uuid-dsym.sh](v0.2.57-strip-uuid-dsym.sh) | [078 — strip-and-debug polish](../docs/sessions/078-strip-and-debug-polish-2026-05-13/README.md) | builds `demos/v0.2.57-strip-uuid-dsym.c` + `demos/v0.2.57-strip-uuid-dsym-helpers.c` two-step under `tcc -gdwarf-2 -c` then linked with `tcc -gdwarf-2`, asserts: `__DWARF` appears before `__LINKEDIT` in the LC table; `__LINKEDIT` covers end-of-file; `LC_UUID` present, non-zero, and identical across rebuilds (deterministic); `dsymutil` produces a `.dSYM` carrying the same `LC_UUID`; the OSO chain has `N_SOL` records (one per function); `strip -S` exits 0; the stripped exe still runs; `LC_UUID` survives the strip; Tiger gdb pairs the stripped exe with its `.dSYM` via UUID and resolves `info functions main` from it; final `OK: __DWARF reorder + page-padding makes strip -S work; LC_UUID round-trips through dsymutil, survives strip, and lets gdb pair the stripped exe with its .dSYM.` | **Strip-and-debug-via-`.dSYM` polish: `strip -S exe` works, and Tiger gdb finds the companion `.dSYM` automatically via `LC_UUID`.** All in `macho_output_exe`: (1) `__DWARF` segment file-laid before `__LINKEDIT` (was: after) and page-padded so `__LINKEDIT.fileoff` lands page-aligned — pre-fix, `strip -S` aborted with "the __LINKEDIT segment does not cover the end of the file (can't be processed)" and dyld faulted on the unaligned `__LINKEDIT` mmap (`doBindIndirectSymbolPointers` at the indirect symbol-table address). VM layout unchanged: `__DWARF` keeps `vmaddr=0`, so `__LINKEDIT.vmaddr` still sits right after `__TEXT+__DATA`. (2) `__LINKEDIT` interior order swapped from `indirect → symtab → strtab` to gcc-4.0's canonical `symtab → indirect → strtab` — Tiger strip enforces it via the "symbol table out of place" assertion. (3) `LC_UUID` (24 bytes total: cmd + cmdsize + 16-byte UUID) always emitted, between `LC_DYSYMTAB` and `LC_UNIXTHREAD`. The 16 UUID bytes come from two FNV-1a 64-bit hashes (different seeds, second pass folding the buffer in reverse to decorrelate) over the post-relocation bytes of `text + rodata + eh_frame + data + init_array + fini_array` and every `__DWARF` section, with RFC 4122 v4 marker bits set. Same source + toolchain → same UUID across rebuilds; `dsymutil` propagates it into the `.dSYM`; gdb pairs by UUID with no manual hookup. (4) Per-function debug-map bracket gained an `N_SOL` record between `N_FUN(name)` and `N_FUN(size)` (gcc-4.0 chain-format parity); doesn't yet fix dsymutil's empty `.debug_info` consolidation (`.debug_pubnames` populates, the rest don't — pre-existing v0.2.56 limitation, deferred to v0.2.58). tests2 111/111, abitest cc+tcc 48/48, bootstrap fixpoint holds; v0.2.37–v0.2.56 debug demos all green. |
| [v0.2.56-n-oso-dsymutil.sh](v0.2.56-n-oso-dsymutil.sh) | [077 — N_OSO + dsymutil-on-Tiger](../docs/sessions/077-n-oso-dsymutil-2026-05-13/README.md) | builds `demos/v0.2.56-n-oso-dsymutil.c` + `demos/v0.2.56-n-oso-dsymutil-helpers.c` two ways (separate `tcc -gstabs -c; tcc -gstabs ...` and separate `tcc -gdwarf-2 -c; tcc -gdwarf-2 ...`), smoke-runs both, then: (a) for the stabs build asserts there are NO N_OSO records (would crash Tiger gdb's .o reader against tcc's `__DWARF,__stab` placement) and that scripted gdb still reads `info line main` + source listing directly out of the linked exe's `LC_SYMTAB`; (b) for the DWARF build asserts six SO/OSO entries (one chain per loaded .o), both OSO records carry the .o's realpath and a non-zero mtime, three BNSYM/ENSYM pairs straddle the three demo functions (`main`/`helper_add`/`helper_mul`) with FUN markers naming each; runs `dsymutil` (which pre-v0.2.56 aborted with `failed assertion '!"Unknown one-operand"'`) and asserts it exits 0, produces a `.dSYM` bundle, and the bundle's `__debug_pubnames` lists all three functions; final `OK: -gstabs flow unchanged (Tiger gdb reads exe-resident stabs); -gdwarf-2 emits an N_OSO debug-map chain dsymutil consumes cleanly.` | **`dsymutil-on-Tiger` consumes tcc-built `-gdwarf-2` Mach-O exes** (roadmap #7.5 Phase 2 item 1b). Three coupled changes in tcc: (1) `ppc-macho.c` records per-loaded-.o OSO state at `.o` load time (realpath via `realpath()`, mtime via `stat()`, `__TEXT,__text` offset + size, plus each defined-in-text function's name + offset from the `.o`'s nlist), gated to DWARF mode only — emitting in stabs mode would let Tiger gdb's OSO follower try to re-read stabs from tcc's `__DWARF,__stab` placement (which differs from gcc's `LC_SYMTAB` nlist placement) and SIGBUS; (2) `emit_stab_nlist` synthesizes a freestanding `N_SO source / N_OSO objpath /  {N_BNSYM N_FUN(name) N_FUN(size) N_ENSYM} per function / N_SO ""` chain in the linked exe's `LC_SYMTAB` per loaded `.o`, with function sizes computed by adjacency (sort by offset; next.off − this.off, last = text_end − this.off); (3) two pre-existing DWARF-2-strictness bugs in `tccdbg.c` fixed under `#if TCC_TARGET_PPC && !TCC_TARGET_PPC64`: (a) `AT_frame_base` now emits `DW_OP_reg31` (r31 = post-prolog FP, set in `gfunc_prolog`) instead of `DW_OP_call_frame_cfa` (a DWARF-3 op Tiger's `dwarf_utilities-42` doesn't recognize and asserts on); (b) `AT_high_pc` (`DW_FORM_data4`) now emits `low_pc + size` (absolute end-VA, with a text-section reloc) instead of `size` (offset form, only valid under DWARF-3+) — pre-fix, a `.o` with multiple functions saw its second-and-onward functions get a `[low,high)` range collapsed to empty, which `dsymutil` silently dropped. Also: `__define_stab (N_OSO, 0x66, "OSO")` added to `tcc/stab.def`; cleanup tracked on TCCState (`macho_oso_states` plus a per-state `funcs[]` array). tests2 111/111, abitest cc+tcc 48/48, bootstrap fixpoint holds; all v0.2.37-v0.2.55 debug-related demos still pass. Stabs-mode flow (`tcc -g` post-v0.2.55, or explicit `-gstabs`) is unchanged from v0.2.55 — gdb reads stabs from `LC_SYMTAB` directly without going through the OSO chain. |
| [v0.2.55-g-default-stabs.sh](v0.2.55-g-default-stabs.sh) | [076 — `-g` defaults to stabs on Darwin](../docs/sessions/076-g-default-stabs-2026-05-13/README.md) | builds `demos/v0.2.55-g-default-stabs.c` with bare `tcc -g` (no `-gstabs`), asserts the resulting exe carries `__DWARF,__stab` + a full `SO/FUN/SLINE/BNSYM/ENSYM` chain in `LC_SYMTAB` and NO `__debug_info` section; rebuilds the same source with explicit `-gdwarf-2` and asserts the inverse (`__debug_info` present, stabs entries absent) — regression check that DWARF is still reachable when asked for explicitly; smoke-runs both exes; scripts `gdb` on the bare-`-g` build to break in `compute`, print params (`x`, `y`), step through locals (`sum`, `prod`), print a global (`g_seed`), walk the backtrace with file:line per frame; final `OK: bare -g now emits stabs by default; -gdwarf-2 still emits DWARF.` | **Bare `tcc -g foo.c -o foo` on PPC Darwin now produces a Tiger-debuggable Mach-O by default** (roadmap #7.5 Phase 2 item 1d). Pre-v0.2.55, plain `-g` emitted DWARF-2 (set by v0.2.39 as the Tiger `dwarfdump`-friendly default); the user had to explicitly type `-gstabs` to get a `gdb 6.3`-debuggable binary, because Tiger gdb reads stabs from `LC_SYMTAB` natively but only partially understands embedded DWARF in Mach-O. v0.2.55 flips the configure default for Darwin: `confvars_set OSX dwarf=2` → `dwarf=0` in `tcc/configure`, which threads through `CONFIG_DWARF_VERSION` → `libtcc.c:2025`'s `s->dwarf = CONFIG_DWARF_VERSION` so bare `-g` sets `s->dwarf = 0` (stabs). One-line source change. The stabs pipeline was made end-to-end robust over the v0.2.51..v0.2.54 arc (one-step and two-step compile-link flows, stack offsets, body markers, cross-TU debugging — all working), so this is the natural default. DWARF is still reachable via explicit `-gdwarf-2` / `-gdwarf-4`, and `-gdwarf`-without-version still uses `DEFAULT_DWARF_VERSION = 2` (`tcc/tcc.h:1933`). tests2 111/111, abitest cc+tcc 24/24, bootstrap fixpoint holds, v0.2.54 cross-TU regression demo still green. |
| [v0.2.54-stabs-roundtrip.sh](v0.2.54-stabs-roundtrip.sh) | [075 — stabs `.o` round-trip](../docs/sessions/075-n-oso-scope-2026-05-13/README.md) | builds `demos/v0.2.54-stabs-roundtrip.c` + `demos/v0.2.54-stabs-roundtrip-helpers.c` separately with `tcc -gstabs -c`, asserts each `.o` contains `__DWARF,__stab` + `__DWARF,__stabstr` and that `__stab` has non-zero relocs; links the two `.o`'s together with `tcc -gstabs` and asserts the linked exe's `LC_SYMTAB` carries N_SO / N_FUN / N_SLINE / N_BNSYM / N_ENSYM entries plus FUN entries for both `main` (in TU 1) and `helper_add`/`helper_mul` (in TU 2); scripts gdb to break in both TUs, print params/locals (`g_seed`, `x`, `y`, `sum`, `product`), and walk the backtrace across the `.o` boundary; final `OK: tcc -gstabs survives the .o round-trip; multi-TU gdb workflow holds.` | **`tcc -gstabs` stabs survive the `.o` round-trip** (roadmap #7.5 Phase 2 item 1b prerequisite). Pre-v0.2.54, `tcc -gstabs -c foo.c -o foo.o` silently dropped the `.stab` / `.stabstr` sections at .o write — `classify_section` had no Mach-O mapping for them, so they fell out of the section table entirely. The one-step compile-and-link flow that v0.2.51 unlocked worked because stabs lived in `s1->stab_section` the whole time; the two-step flow real Makefiles use was not debuggable. v0.2.54 closes that gap with four coupled changes in `ppc-macho.c`: (a) `classify_section` adds `.stab` → `__DWARF,__stab` and `.stabstr` → `__DWARF,__stabstr` with `S_ATTR_DEBUG`, mirroring the existing `.debug_*` convention; (b) `macho_section_to_elf` adds the reverse mapping so the .o reader reattaches them to the merged `stab_section` + `.stabstr`; (c) `emit_section_relocs` lets STT_SECTION-targeted relocs through to the local-handling path so the per-stab anchor relocs (against `text_section`'s section_sym) round-trip — they're not in the Mach-O nlist xlat table but the section number suffices; (d) a new Pass 1.5 in `macho_load_object_file` walks the just-loaded `.stab` entries and adds the merged-`.stabstr` offset to each non-zero `n_strx`. Two bugs shaken out along the way: (1) once `__DWARF,__stab` is mapped, its `addr=0` overlaps the `__nl_symbol_ptr` slot range in the SECTDIFF target-section search, breaking PIC data references to extern globals — the loader now skips `S_ATTR_DEBUG` sections during that walk; (2) multi-`.o` round-trips contribute one leading empty Stab_Sym placeholder per `.o`, which `emit_stab_nlist` would emit as junk — now skipped when `n_type == 0`. Result: cross-`.o` gdb-on-Tiger works end-to-end through the two-step flow. tests2 111/111, abitest-cc/tcc 24/24, bootstrap fixpoint holds. |
| [v0.2.53-bnsym-ensym.sh](v0.2.53-bnsym-ensym.sh) | [074 — BNSYM/ENSYM / Phase 2 1c](../docs/sessions/074-bnsym-ensym-2026-05-13/README.md) | builds `demos/v0.2.53-bnsym-ensym.c` with `tcc -gstabs`; runs `nm -ap` and asserts a paired `BNSYM`/`ENSYM` straddles every user function (`tiny`/`small`/`medium`/`stat_helper`/`main`) plus the tcc-emitted `__keymgr_*` helpers, with the opening `BNSYM` address equal to the function's `N_FUN` address and `BNSYM` count equal to `ENSYM` count; re-runs the v0.2.52 PSYM/LSYM gdb workflow on the same binary as a regression check (`break medium`, step through `t1`/`t2`/`t3`/`t4` and assert values); final `OK: tcc -gstabs emits paired BNSYM/ENSYM around every function.` | **Apple-conventional `N_BNSYM` (0x2e) / `N_ENSYM` (0x4e) markers around every function body** (roadmap #7.5 Phase 2, item 1c). gcc-4.0 emits these around each `N_FUN` entry to pin the body's start/end addresses explicitly; tcc didn't, until v0.2.53. Implementation: two `put_stabs_r` calls in `tccdbg.c` (BNSYM in `tcc_debug_funcstart` before the opening N_FUN at `value=func_ind`; ENSYM in `tcc_debug_funcend` after the closing N_FUN size-entry at `value=func_ind+size`), both gated by `#ifdef TCC_TARGET_MACHO` and reloc'd against `text_section`/`section_sym`. Both constants added to `tcc/stab.def` with Apple's published values. The A/B comparison against v0.2.52 found that **Tiger gdb 6.3 itself shows no observable behavior change** — `break <fn>`, `info line <fn>`, `info function <fn>`, single-step through prolog, and disassembly all produce identical output with vs without the markers. The handoff anticipated a prolog-skip improvement (`break helper` landing past the entry); empirically gdb 6.3's existing SLINE-based heuristic already finds the body's first source line cleanly on PPC. So 1c is convention/correctness-level work: tcc's stabs output now matches gcc-4.0's exact shape on the same target, which is **infrastructure for Phase 2 item 1b** (`N_OSO` + `dsymutil` round-tripping, where dsymutil walks the BNSYM/N_FUN/ENSYM sequence to scope a function's debug info). Regression: tests2 111/111, abitest 24+24, bootstrap fixpoint holds; v0.2.52 PSYM/LSYM gdb workflow unaffected. |
| [v0.2.52-gstabs-locals.sh](v0.2.52-gstabs-locals.sh) | [073 — gstabs locals / Phase 2 1a](../docs/sessions/073-gstabs-locals-2026-05-13/README.md) | builds `demos/v0.2.52-gstabs-locals.c` with `tcc -gstabs`; dumps PSYM/LSYM offsets via `nm -ap`; scripts `gdb` to break inside `add3` / `with_local_and_global` / `many_locals`, print every parameter on entry, step through each local assignment and print the just-assigned local, then `bt` to assert `add3 (a=10, b=20, c=30)` and `main (argc=1, argv=0x...)` both appear with real values; final `OK: tcc -gstabs PSYM/LSYM offsets read correctly under gdb.` | **`print <param>` / `print <local>` now read the right values from gdb on Tiger** (roadmap #7.5 Phase 2, item 1a). Pre-v0.2.52, v0.2.51's interactive-debug story was "you can step but you can't see your values": `print x` inside `helper(int x)` returned garbage (e.g. `-1881102064` — an address inside the function's outgoing-param area) because tcc's `N_PSYM` / `N_LSYM` `n_value` was r31 (FP) relative (`s->c = +24` for the first arg, `s->c = -12` and lower for locals), while Tiger gdb 6.3 on PPC interprets stabs offsets as r1 (post-prolog SP) relative — exactly the gcc-4.0 convention. Since the PPC prolog sets `r31 = r1 + frame_size`, the fix is a single arithmetic shift at emit time: in `tccdbg.c::tcc_debug_finish` (which runs from `tcc_debug_funcend` immediately after `gfunc_epilog` has stashed the just-computed `frame_size` in `ppc_last_frame_size`), add `ppc_last_frame_size` to every `N_PSYM` value and to every nonzero `N_LSYM` value before calling `put_stabs`. The nonzero guard exempts type-def LSYMs (struct fields / typedefs emitted by `tcc_get_debug_info` while inside the function carry `value == 0` unambiguously — PPC params start at +24 and locals at -12, never zero). Gated by `#if defined(TCC_TARGET_PPC) && defined(TCC_TARGET_MACHO)`; DWARF is untouched because the DWARF path uses `DW_OP_fbreg <s->value>` against a frame-base of `DW_OP_call_frame_cfa`, and v0.2.39's CFI already defines CFA as `r1 + frame_size` (so DWARF gets the right result out of the box). After the fix, both stepped-locals (`$1 = 10`, `$2 = 20`, `$3 = 30`, then `next` → `$4 = 30`, then `next` → `$5 = 60`) and stack-walk frame values (`add3 (a=10, b=20, c=30)` / `main (argc=1, argv=0xbffffbe0)`) work end-to-end. tests2 111/111, abitest-cc 24/24, abitest-tcc 24/24, bootstrap fixpoint holds; the v0.2.51 demo also still passes and now gets the bonus of real argument values in its backtrace frames. |
| [v0.2.51-gstabs-oso-stab.sh](v0.2.51-gstabs-oso-stab.sh) | [072 — OSO STAB / gdb-on-Tiger](../docs/sessions/072-oso-stab-gdb-tiger-2026-05-13/README.md) | builds `demos/v0.2.51-gstabs-oso-stab.c` with `tcc -gstabs`; dumps the embedded STAB chain via `nm -ap`; scripts `gdb` to set breakpoints by function name and by line, run the program, hit `main`, print `g_global`/`g_static`, continue into `helper`, and walk the backtrace; final `OK: tcc -gstabs produces gdb-debuggable Mach-O on Tiger.` | **`tcc -gstabs` produces an interactively-debuggable Mach-O on Tiger PPC** (roadmap #7.5). tcc's existing GNU-stabs+ emission path (under `-gstabs`) already populated `stab_section` during compilation with the right entries, but the Mach-O writer historically dropped them — so pre-v0.2.51 the linked exe ran identically to a non-debug build and `gdb` only saw external function entries (no file/line info, no source listing). Fix is in two parts: (a) a new `emit_stab_nlist()` helper in `ppc-macho.c` walks `stab_section` (and its companion reloc table) at link time and emits one Apple `nlist` entry per Stab_Sym into the local-symbol range of LC_SYMTAB, translating `n_value` via the laid-out section vmaddrs + the target sym's `st_value` so n_value carries the runtime VA (function address, line address, static-var address) and `n_sect` carries the 1-based Mach-O section ordinal; (b) tccdbg.c's `N_SLINE` / `N_LBRAC` / `N_RBRAC` emission switches to *absolute*-address form (`put_stabs_r` with `text_section` + `section_sym`, gated by `#ifdef TCC_TARGET_MACHO`) since Apple's debug-map convention uses absolute VAs throughout, not the GNU function-relative offsets — plus a paired `N_FUN` end-entry (empty name, `n_value=size`) so dsymutil / gdb get unambiguous function boundaries. With this, `gdb` on Tiger sets `break main` / `break helper` / `break <line>` by file:line, `run` produces a breakpoint hit with `file:line` displayed, `bt` walks the stack with file:line per frame, `list` reads the source file referenced by `N_SO`, and `print <global>` works against the runtime address. Variable inspection through stack offsets (`print x`, `print local`) is wrong because tcc's `N_PSYM` / `N_LSYM` offsets don't match where the PPC backend actually stores parameters/locals in the frame — that's a Phase-2 polish item tracked in the session 072 HANDOFF, along with N_OSO emission for `dsymutil` round-tripping. tests2 still 111/111, abitest-cc 24/24, abitest-tcc 24/24, bootstrap fixpoint holds; non-debug builds and the default DWARF debug path are unchanged (the change is gated by `s1->do_debug && s1->dwarf == 0` in the helper and `#ifdef TCC_TARGET_MACHO` in tccdbg.c). |
| [v0.2.49-clz-ctz-ub.sh](v0.2.49-clz-ctz-ub.sh) | [067 — libtcc1 clz/ctz](../docs/sessions/067-libtcc1-clz-ctz-2026-05-11/README.md) | calls `__builtin_clz/ctz/clzl/ctzl/clzll/ctzll` on `x==0` and asserts the post-fix tcc returns the gcc-4.0-on-PPC observed values (clz(0)=32, ctz(0)=-1, clzll(0)=64, ctzll(0)=31); also smoke-checks the non-zero (defined-behavior) and clrsb(0)/clrsb(-1) paths; prints `All PASS` over 18 sub-tests | **libtcc1.a `__builtin_clz` / `__builtin_ctz` (and the long / long-long variants) match gcc-PPC at the UB input `x==0`.** Pre-v0.2.49, tcc's libtcc1.a used a de-Bruijn-table fallback that returned 31 / 0 / 63 / 0 for `clz(0)` / `ctz(0)` / `clzll(0)` / `ctzll(0)` — internally consistent and conformant (the gcc spec marks these inputs as undefined behavior), but persistently divergent from gcc-4.0's `cntlzw`-inline + libgcc `__ctzdi2` values of 32 / -1 / 64 / 31. Csmith `--builtins` campaigns emitted `clz(0)/ctz(0)` patterns freely, which made gcc-vs-tcc differential output diverge; session 062 papered over it with a `-include builtin_compat.h` UB-wrapper shim. Fix: one short-circuit per function body in `tcc/lib/builtin.c` — `if (!x) return XX;` ahead of the de-Bruijn lookup. `clrsb()` deliberately keeps the bare `CLZI`/`CLZL` macros (clrsb(0)=31 / clrsbll(0)=63 are well-defined per the gcc spec and the de-Bruijn path already produces them). |
| [v0.2.48-r12-clobber.sh](v0.2.48-r12-clobber.sh) | [066 — seed-732 r12 fix](../docs/sessions/066-seed-732-2026-05-11/README.md) | requires the csmith runtime headers (defaults to `/Users/macuser/tmp/csmith`); compiles the unmodified csmith default-opts seed-732 program with tcc and runs it; checks the end-of-run checksum is `76F5DB56` (matches gcc-4.0 -O0); pre-fix tcc produced `7F5F6E2D`; prints `PASS  checksum = 76F5DB56` | **r12 reg-allocator clobber fix.** Pre-v0.2.48, tcc's int register allocator (slot 9 = r12) was hardcoded as scratch by every two-instruction global-symbol load (`lis r12, ha(sym); lwz/lbz rD, lo(sym)(r12)`) AND simultaneously in the allocator pool. Under high register pressure the allocator placed a live vstack value in r12; the next `lis r12, ha` silently destroyed it. Csmith default-opts seed 732 surfaced this inside func_8's giant `(*l_528) = !(safe_div(...))` expression — `(*l_505) |= g_26.f0` parked the lhs byte (0xA4 → r12), the rhs `g_26.f0` load issued `lis r12, 0x2`, clobbering r12, then `or r12, r12, r3` produced `0x20000 \| 7 = 0x20007` stored as byte 0x07 instead of 0xA7. End-of-run checksum diverged from gcc-4.0 -O0 at the first `g_359.f0` hash. Fix: removed r12 from `reg_classes[]` (slot 9 = `0` instead of `RC_INT \| RC_R(9)`); tcc's int pool drops from 10 to 9 slots (r3..r11), r12 becomes true scratch, the lis-clobber-class is closed. Verified by the same 1000-seed default-opts campaign that surfaced the bug (session 065: 1 FAIL → session 066: 0 FAIL). |
| [v0.2.47-fp-arg-shadow.sh](v0.2.47-fp-arg-shadow.sh) | [058 — csmith followups](../docs/sessions/058-followups-2026-05-09/README.md) | exercises 8 call shapes that mix a compound-assignment-with-call int arg (`(x &=/^=/\|=/+=/-= helper(...))`) with a float / double / two-float second arg; pre-fix, the FP arg's variadic GPR-shadow `lwz` clobbered the int arg's value, so the int arg printed as `1084227584` (= 0x40A00000 = float 5.0 bit pattern); post-fix all 8 receive the correct int value; prints `All PASS` | **FP-arg GPR-shadow no longer clobbers a previously computed int arg.** Pre-v0.2.47, `gfunc_call`'s FP-arg variadic GPR-shadow code emitted `lwz r{gslot+3}, 16(r1)` (and double / straddle variants) directly to a hardcoded register without first calling `save_reg()`. If a previously-evaluated arg's value happened to live in that GPR, the shadow load clobbered it, and arg setup later in pass 2 emitted `mr r3, r{shadow}` — putting the float bit pattern in r3 where the int arg should have been. Surfaced via csmith --float seed 3228 (`(x &= helper(...))` with `helper` returning into r3 and `&=` result landing in r4, then a float arg2 whose shadow targets r4). Fix: call `save_reg(gslot)` (and `gslot+1` for VT_DOUBLE both-in-GPR) before the shadow `lwz`. Mirrors the existing LL straddle / both-in-GPR paths. |
| [v0.2.46-float-int-const-fold.sh](v0.2.46-float-int-const-fold.sh) | [056 — csmith bug-hunt](../docs/sessions/056-csmith-2026-05-09/README.md) | runs 9 cases checking compile-time float-to-int conversion saturates to PPC `fctiwz` semantics: huge positive single → INT_MAX, huge negative → INT_MIN, in-range round-toward-zero, char/short post-mask correctly truncates the saturated int32, and runtime+const-fold agree on the same source; prints `All PASS` | **Float-to-int constant-fold matches PPC fctiwz semantics.** Pre-v0.2.46, tcc's compile-time const folder used host `(int64_t)long_double` (libgcc `__fix*di`), which doesn't saturate to int32 range. So `int x = (int)1e10f` folded to `1410065408` (low 32 bits of `(int64_t)1e10`) while runtime tcc emitted `fctiwz` and got `0x7FFFFFFF` (INT_MAX) — same source, two different answers. Surfaced via csmith differential testing (`--float --builtins`, seed 92): `func_65` had a chained `*(int*)p = (..., huge_float_const)` pattern where `g_99` ended at `-1` under tcc and `INT_MAX` under gcc-4.0. Fix: in tccgen.c's `gen_cast` constant-fold branch, on PPC target, emulate `fctiwz` for byte/short/int targets — saturate positive overflow to `0x7FFFFFFF`, negative overflow / NaN to `0x80000000`, otherwise round-toward-zero. VT_LLONG keeps the existing path (runtime there already goes through libgcc `__fixsfdi`/`__fixdfdi` whose overflow semantics match the host helper). |
| [v0.2.45-be-bitfield-abi.sh](v0.2.45-be-bitfield-abi.sh) | [055 — bug-hunt](../docs/sessions/055-bughunt-2026-05-09/README.md) | builds a small program that exercises three previously-broken bitfield access patterns on PPC BE: union-overlay (`union {uint32_t f0; unsigned f1:14;}`), nested-struct in union, and pointer-cast access; verifies `u.f1` reads `(v >> 18) & 0x3fff` (standard SysV/AIX PPC BE bitfield ABI: MSB-first within container); also checks `f1=0x92` writes `0x02480000` to the sibling uint; runs 6 additional values; prints `All PASS` | **BE PPC bitfield ABI fix.** Pre-v0.2.45, tcc on PPC laid out bitfields LSB-first within the storage container, while gcc-4.0 and the PowerPC SysV/AIX ABI use MSB-first. Internally tcc was consistent (struct write + read round-trips), but any path that mixed plain-int writes with bitfield reads of the same memory (sibling union member, pointer cast, gcc-built object on the same struct, on-disk data using the standard ABI) saw a byte-swapped value. Found via csmith differential testing — seed 3 with default csmith options diverged at `g_55.f1` (`union U1 {uint32_t f0; unsigned f1:14; ...}`). Fix: BE branches in tccgen.c's three byte-wise bitfield paths (`load_packed_bf`, `store_packed_bf`, `init_putv` static initializer) interpret bit_pos as MSB-first and walk bytes from the container's MSB end (low memory address on BE) toward the LSB end, accumulating bits MSB-first in the result. struct_decl's bit_pos allocation is unchanged — it's a running counter starting at 0 and the access-side reinterpretation is enough. Tests2 still 111/111; abitest-cc 22/22; abitest-tcc 22/22; bootstrap fixpoint holds. |
| [v0.2.44-gdebug-link.sh](v0.2.44-gdebug-link.sh) | [054 — unsupervised](../docs/sessions/054-unsupervised-2026-05-09/README.md) | compiles a 3-TU `extern int v; int *p = &v;` test with `-g` to .o files, links them, runs the exe and checks `*p == 42`; then a 3-TU function-pointer indirect-call test (extern function, function-pointer table init, indirect call sequence) with `-g`, checks the accumulated counter; prints `PASS` | **`tcc -g` linking separately-compiled .o files now works.** Pre-v0.2.44, after v0.2.43 made `tcc -g -c` no longer crash, the link step still produced SEGV-ing exes for cross-TU extern refs. With -g, debug sections (`__debug_info` etc.) start at addr=0 with large sizes and shadowed real `__nl_symbol_ptr` (addr 0x180 size 4) in the SECTDIFF target-section lookup in `macho_translate_relocs`. The wrong section was matched, `macho_resolve_stub_slot` returned -1, and the SECTDIFF reloc was silently dropped — the .o-internal displacement remained baked into the linked exe. Fix: filter the section search to real merged sections + stub-like sections (S_SYMBOL_STUBS / S_LAZY_SYMBOL_POINTERS / S_NON_LAZY_SYMBOL_POINTERS); debug sections are skipped. Side effect: the v0.2.42 Python demo can now drop its `-g`-stripping workaround. |
| [v0.2.43-gdebug-extern.sh](v0.2.43-gdebug-extern.sh) | [054 — unsupervised](../docs/sessions/054-unsupervised-2026-05-09/README.md) | exercises 3 patterns that previously SIGBUSed `tcc -g`: `extern int v; int *p = &v;` (extern data ref), an `extern void f(); struct {...; void(*)(); } table[] = {...};` array, and a mixed extern-data + extern-function struct array; verifies all compile, all have a `__DWARF` segment, all parse cleanly with `dwarfdump`; runs a single-source link-and-execute test for the data-pointer case; prints `PASS` | **`tcc -g` no longer SIGBUSes on extern-sym refs in static initializers**. Pre-v0.2.43, compiling a static initializer that referenced an extern symbol with `-g` enabled crashed in `ppc-macho.c::classify_sym` with a NULL deref. Surfaced by Python 2.7.18's `Modules/config.c` (mass `extern void initX(void);` declarations + a static `_PyImport_Inittab[]` initializer). One-line fix: NULL-check matching the four other identical loops in the same file. Note: a separate latent `-g` regression remains in the `.o`-then-link path; documented in roadmap. |
| [v0.2.42-python.sh](v0.2.42-python.sh) | [054 — unsupervised](../docs/sessions/054-unsupervised-2026-05-09/README.md) | downloads + builds Python 2.7.18 with `CC=tcc` (~150 .c files, several minutes on a G3); confirms `libSystem`-only linkage; runs a Python program exercising list comprehensions, recursion, classes/methods, string ops, dict walks, exception handling, float formatting; prints `PASS` | **Python 2.7.18 builds + runs end-to-end with tcc** — eighth real-world program after lua, zlib, bzip2, cJSON, sqlite, sed, gzip. Pre-v0.2.41, tcc-built python.exe died at startup with `Fatal Python error: Can't initialize type type`; v0.2.41's `extra_off`-truncation fix in the PIC load/store paths unblocked it as a side effect. PyTypeObject is large (220+ bytes), its slot offsets live past the 16-bit-signed displacement range pre-v0.2.41 tcc was masking, so PyType_Ready read garbage from the wrong fields and bailed. The demo strips `-g` from CFLAGS as a workaround for a newly-discovered tcc bug — emitting DWARF for a static initializer that references an extern symbol (minimal repro `extern int v; int *p = &v;`) SIGBUSes — filed for next session. |
| [v0.2.41-gzip.sh](v0.2.41-gzip.sh) | [054 — unsupervised](../docs/sessions/054-unsupervised-2026-05-09/README.md) | downloads + builds + runs GNU gzip 1.11; rounds-trips inputs from 1 KB to 500 KB plus `/etc/services` (~570 KB) through both tcc-built gzip self-decompression AND system `/usr/bin/gunzip`; confirms `CC=tcc` and `libSystem`-only linkage; prints `PASS` | **GNU gzip 1.11 builds + round-trips end-to-end with tcc** — seventh real-world program. Pre-v0.2.41, gzip-tcc could compress and self-decompress inputs up to ~63 KB, but at 64 KB+ the system `/usr/bin/gunzip` rejected the output with CRC errors and tcc-gzip's self-decompression also produced wrong bytes. The bug was in tcc's PIC load/store paths: `(extra_off & 0xffff)` truncated the byte offset of `head[n]` references in `fill_window`'s slide loop. Gnulib's gzip uses `#define head (prev+WSIZE)` with `WSIZE=32768`; `head[n]` decomposes to `*(prev + WSIZE + n)`, byte offset `WSIZE * sizeof(Pos) = 0x10000`. tcc's PIC load with extra_off=0x10000 emitted `lwz r, 0(addr_gpr)` (the high bits dropped on the floor), so the slide loops read/wrote `prev[]` instead of `head[]`. fill_window's slide is triggered when `strstart >= WSIZE+MAX_DIST = 65274`, i.e. when input crosses the 64 KB threshold — explaining why <= 63 KB worked. Fix: `ppc_adjust_extra_off` helper that emits an `addis addr_gpr, addr_gpr, ha16(extra_off)` for offsets that don't fit in the 16-bit signed D-form immediate, returning the lo16 portion. Applied at the top of all four affected PIC paths in `load()` and `store()`. |
| [v0.2.36-builtin-fma.sh](v0.2.36-builtin-fma.sh) | [051 — fmadd codegen](../docs/sessions/051-fmadd-2026-05-07/README.md) | compiles a function using `__builtin_fma` and `__builtin_fmaf` with tcc; greps the disasm to confirm tcc emitted `fmadd`/`fmadds` inline (and not a fallback `bl _fma`); runs 4 algebraic invariants; checks libtcc1.a now contains `fmadd` (because `dd_two_prod` uses it) | **`__builtin_fma` / `__builtin_fmaf` emit PPC hardware `fmadd`/`fmadds` inline.** Pre-fix, calls to `__builtin_fma()` died at runtime with "Symbol not found." v0.2.36 adds the tokens and dispatches them in tccgen.c's expression parser, calling into a small `gen_ppc_fmadd` helper in ppc-gen.c. Side benefit: lib-ppc.c's `dd_two_prod` (used by all `__gcc_q*` arithmetic from v0.2.34) collapses from 10 flops to 2 (`p = a*b; e = fma(a, b, -p)`); all v0.2.34 invariants still hold. |
| [v0.2.35-libm-ldbl128.sh](v0.2.35-libm-ldbl128.sh) | [050 — libm LDBL128 redirect](../docs/sessions/050-libm-ldbl128-2026-05-07/README.md) | builds with tcc and gcc; verifies tcc references `_ldexpl$LDBL128` (the 16-byte-LD libm entry); runs 4 invariants exercising `ldexpl` / `scalbnl` / `fabsl`; expects both to print `PASS` | **libm LDBL128 redirect for math functions.** v0.2.32 redirected stdio.h symbols to `*$LDBL128`; this completes the work for `<math.h>`. Pre-fix, `ldexpl` etc. went to the legacy 8-byte-LD entries that treat LD as a plain double — `ldexpl(136, -1030)` returned 136 unchanged. Most visibly: tcctest's `0x0.88p-1022` subnormal sub-test was parsing as 136 instead of ~1.18e-308 because parse_number's internal `ldexpl` call was a no-op. Fix: predefine `__LONG_DOUBLE_128__=1` so `<architecture/ppc/math.h>`'s `__LIBMLDBL_COMPAT` macro emits the `$LDBL128` symbol redirect. `make test3` content-diff drops 45 → 13 lines (remaining structural). |
| [v0.2.34-dd-precision.sh](v0.2.34-dd-precision.sh) | [049 — dd precision](../docs/sessions/049-dd-precision-2026-05-07/README.md) | runs 5 standard dd-precision invariants (2^53+1 round-trip; 1e16+1−1e16=1; 1e20+1≠1e20+2; pi*pi keeps low-half rounding error; 100/7*7 closes to ~10^-30) under both tcc and gcc; expects both to pass and exit 0 | **Precision-correct double-double arithmetic.** v0.2.32's LD ABI used lossy `__gcc_q*` / tf2 stubs (high-double only, low-half always zero) — programs needing >53-bit mantissa silently lost precision: `(2^53 + 1) - 2^53` returned 0. v0.2.34 replaces the stubs with Knuth Two-Sum + Veltkamp split + Dekker product (the standard libgcc soft-fp dd algorithms). No fma dependency. tf2 compares are now lexicographic over `(hi, lo)` — exact for canonical dd values. |
| [v0.2.33-libtcc-callback.sh](v0.2.33-libtcc-callback.sh) | [048 — libtcc REL24 fix](../docs/sessions/048-libtest-rel24-2026-05-07/README.md) | small libtcc client registers `add()` callback via `tcc_add_symbol`, JIT-compiles a function that calls it, runs it, asserts the result; prints `PASS` | **libtcc callback (SHN_ABS) reachability.** Pre-v0.2.33 a JIT-emitted `bl _add` failed with `R_PPC_REL24 out of range` when the JIT region (mmap'd somewhere) landed >32MB from the libtcc binary's `add` function. `build_got_entries` bypassed PLT/GOT synthesis for SHN_ABS symbols whenever `PTR_SIZE != 8` (under the assumption that 32-bit platforms can always reach absolute symbols via direct REL24) — but PPC32's 24-bit branch range is only ±32MB, easily exceeded. Fix: include `TCC_TARGET_PPC` in the same exception list as `TCC_TARGET_ARM`. The existing PPC `create_plt_entry` (4-insn `lis/ori/mtctr/bctr`) and `relocate_plt` machinery handles emission unchanged. Closes the upstream `libtest` stage (mistakenly listed as passing in session 046's HANDOFF — verified failing both pre- and post-v0.2.32 by stash-and-retest). |
| [v0.2.32-longdouble.sh](v0.2.32-longdouble.sh) | [047 — long double = double-double](../docs/sessions/047-longdouble-2026-05-07/README.md) | builds a stress program that mixes the previously-broken cases (LD param/return, LD interleaved with int args, LD variadic to printf, LD comparisons); compares tcc output against gcc-4.0 reference; prints `PASS` when they match | **`long double` = IBM double-double on PPC Darwin.** Pre-v0.2.32, tcc treated `long double` as 8-byte double (LDOUBLE_SIZE = 8), violating Apple PPC32's 16-byte IBM-double-double ABI. Mixing tcc-built and gcc-built code over the LD ABI silently corrupted values (`abitest-cc::ret_longdouble_test` and `arg_align_test` had been failing since v0.2.0). v0.2.32 makes LD a true 2-FPR-pair value: `LDOUBLE_SIZE=16`, `LDOUBLE_ALIGN=4`, `R2_RET(VT_LDOUBLE)=REG_FRE2`. Codegen routes LD arithmetic through `__gcc_q{add,sub,mul,div}` (lossy stubs), comparisons through `__{eq,ne,lt,le,gt,ge}tf2`, ABI-pair pass/return through stfd-then-lfd via the outgoing param area. Predefines `__LDBL_MANT_DIG__=106` so Tiger's `<sys/cdefs.h>` redirects `printf`/`scanf` to `printf$LDBL128`/etc. for correct 16-byte `%Lf` reads. **Closes the last two `abitest-cc` failures (now 24/24).** Bootstrap fixpoint and tests2 (111/111) hold. |

## How to run any demo

On imacg3 (or any Tiger PPC G3+):

```sh
ssh imacg3
cd ~/tcc-darwin8-ppc
./scripts/build-tiger.sh        # if you haven't built yet
cd tcc
./tcc -B. -run ../demos/sNNN-<slug>.c
echo $?                          # check the exit code matches the table
```

The `-B.` points tcc at the current directory for `libtcc1.a` (an
empty archive at present, but tcc still wants to find it). After we
ship the first /opt-installable tarball, `tcc demo.c -o demo && ./demo`
will work without `-B.`.
