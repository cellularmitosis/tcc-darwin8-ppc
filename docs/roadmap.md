# Roadmap

## Where we are (May 2026)

Self-hosted tcc for Tiger PowerPC, **shipped through v0.2.66-g3**.
Forty-four patch releases on top of [v0.2.0-g3](sessions/028-v0.2.0-g3-release/README.md)
have brought tests2 from 70 / 122 (57%) to **111 / 111 (100%)**:

| | tests2 | what it added |
|---|---|---|
| [v0.2.66-g3](sessions/089-real-world-awk-2026-05-16/README.md) | 111 / 111 | **Math-builtin prototypes for Tiger's `<math.h>` inline `isinf` / `isnan` / `isfinite` path — one-true-awk (Brian Kernighan's awk) builds and runs correctly with tcc as CC.** Tiger's `/usr/include/architecture/ppc/math.h` implements `isinf(x)` / `isnan(x)` / `isfinite(x)` as macros that dispatch on `sizeof(x)` to inline functions of the form `__builtin_fabs(x) == __builtin_inf()` (etc.). gcc treats `__builtin_fabs` / `__builtin_inf` as compiler intrinsics that inline to FP register checks; tcc emits a regular function call into libtcc1.a's wrappers (`lib/lib-ppc.c::__builtin_fabs` / `__builtin_inf`, added in v0.2.21). But tcc never *predeclared* their prototypes, so every caller of `isinf` / `isnan` / `isfinite` saw the inline body call `__builtin_fabs(x)` with an implicit-int return signature. On PowerPC the ABI passes doubles in `f1` and ints in `r3`; the implicit-int caller reads `r3` (garbage) instead of `f1` (the real result). Net effect: `isinf(0.0)` returned 1, `isinf(+inf)` returned 0, every `isinf`/`isnan`/`isfinite` check on a numeric value was *silently lying* — but the wrong values happened to compare equal under "==" often enough that callers like sed / gzip / lua never tripped a visible failure. awk's `tran.c::get_inf_nan()` is called from `get_str_val()` whenever a numeric Cell is stringified for output; under tcc-built awk, `print NF` on an empty record (NF=0) printed `+inf` instead of `0`. A direct REGRESS testsuite comparison between gcc-built and tcc-built one-true-awk surfaced 22 `t.*`/`p.*` divergences (`t.NF`, `t.arith`, `t.cum`, `t.cat`, `t.coerce2`, `t.builtins`, `t.makef`, `t.ofmt`, `t.rec`, `t.split8`, `t.6.x`, `t.d.x`, `t.getval`, `t.i.x`, `t.incr2`, `t.incr3`, `t.j.x`, `t.set0`, `t.set0a`, `t.set3`, `t.vf2`, `p.44`) all rooted in the same bug. **Fix:** a six-line block in `tcc/include/tccdefs.h`'s `__APPLE__` branch (right next to the existing `__builtin_huge_val()` / `__builtin_nanf` / `__builtin_flt_rounds` / `__builtin_bzero` predefs) adds `extern` declarations for `__builtin_fabs` / `__builtin_fabsf` / `__builtin_fabsl` / `__builtin_inf` / `__builtin_inff` / `__builtin_infl`. With the prototypes in scope, tcc emits the right calling convention (double return in `f1`, long-double return in `f1:f2`), and the existing libtcc1.a wrappers from v0.2.21 keep providing the link symbols. Post-fix REGRESS run with `oldawk=./awk-gcc awk=./awk-tcc` shows **zero `t.*`/`p.*` divergences** — gcc-built and tcc-built one-true-awk are now byte-identical on every comparison the test corpus performs. Remaining "test N.M failed" lines (70 of them) are identical to the gcc-baseline-vs-Tiger-system-awk run, i.e. inherent test issues (T.utf locale, T.overflow ulimit, T.argv hardcoded `../a.out` path) — not tcc-related. The bug also explains why `__builtin_isnan` / `__builtin_isinf` (added as int-returning wrappers in the same v0.2.21 commit) *appeared* to work: their `int` return matches tcc's implicit-int assumption, so the calling convention happens to align by accident. tests2 111/111, abitest cc 24 + tcc 24, bootstrap fixpoint holds; all v0.2.12..v0.2.65 demos still PASS (lua, bzip2, cjson, sed with `[[:class:]]`, gzip, dylib-slide, alacarte, gdebug-extern, gdebug-link, v0.2.63-extern-data-ref, v0.2.64-funcptr-const, v0.2.65-rodata-data-const). Demo: [`v0.2.66-awk.sh`](../demos/v0.2.66-awk.sh) — downloads one-true-awk tag 20260426, installs bison-3.8.2 via `tiger.sh` if missing, builds awk with tcc as CC, asserts only `libSystem.B.dylib` is linked, runs five smoke tests including the smoking gun (`printf "\n" \| awk '{print NF}'` should print `0`, was `+inf` pre-fix). |
| [v0.2.65-g3](sessions/088-rodata-reclass-data-const-2026-05-15/README.md) | 111 / 111 | **`.rodata` reclassification to `__DATA,__const` — gcc-4.0 pointer-equality parity for static-const function-pointer tables.** v0.2.64's stub-trick fix made the table entries callable (sed's POSIX character classes went from SIGSEGV to working) but left the slot value as the stub VA, while `fn_t q = extern_fn` in expression code resolved through `__nl_symbol_ptr` to the libSystem function VA. The two pointers compared unequal even though both reach the same function at runtime — `prednames[0] == &isalpha` was false under tcc, true under gcc-4.0. v0.2.65 closes the gap by mirroring gcc-4.0's section-placement convention: any `.rodata` carrying `R_PPC_ADDR32` against an undef sym moves at link time from `__TEXT,__const` (read-only, dyld can't bind there) to `__DATA,__const` (writable at load time, between `__dyld` and `__bss` per Apple's `__DATA` ordering); classic-format external relocations (`r_extern=1, r_length=long, r_type=PPC_RELOC_VANILLA, r_pcrel=0`) get emitted for each matching slot, and Tiger's dyld writes the bound target VA in at load time. Three coupled changes in `tcc/ppc-macho.c`'s EXE writer: (a) `rodata_in_data` decided once right after the section-finding pass, gated on any undef-ADDR32 in rodata's reloc table — defined-target ADDR32s stay happily in `__TEXT,__const` since they're link-time-baked. (b) the section count, `__TEXT` / `__DATA` layout passes, LC_SEGMENT cmds and the file-write loop all split on `rodata_in_data`: the rodata block moves from after `__text` in `__TEXT` to between `__dyld` and `__bss` in `__DATA`, and the file payload moves with it so file offsets stay monotonic (the file-write bug from the first build — appending rodata BEFORE stubs in the file even though its file_off was now higher, yielding all-zero stubs and SIGILL — taught us that). (c) `exe_resolve_section_relocs`'s `wants_extrel` predicate widened from `(UNDEF && !STT_FUNC && !has_stub)` to just `(UNDEF)` so STT_FUNC undef ADDR32 in writable sections goes through extrel too — `static fn_t global_q = extern_fn;` in `.data` now also reaches gcc parity (was stub VA, now libSystem VA), and the same widening lets the moved-rodata path bind STT_FUNC undefs cleanly. The stub allocated by `collect_extern_stubs` (heuristics (a) REL24 and (b) rodata-ADDR32) remains useful for REL24 callers; the ADDR32 path bypasses it and writes the dyld-bound libSystem VA directly into the slot. v0.2.64's heuristic (b) is now functionally dead for the EXE path but stays in place because the dylib writer still relies on the v0.2.64 stub trick for its own `__TEXT,__const` (deferred). The dylib writer is unchanged this session — its dylib extrel emission is the natural follow-up (handoff #2). The widened `wants_extrel` predicate also closes the long-deferred exotic case from session 087: `const T *const p = &extern_data;` (rare, but) now resolves to the libSystem data address via extrel instead of the slot address or stub address fallbacks. Verified end-to-end with a fresh repro: `static const fn_t prednames[] = {isalpha}; static fn_t global_q = isalpha;` plus a `fn_t local_q = isalpha;` local var — all three resolve to `0x900b13dc` (libSystem `_isalpha`'s VA on Tiger), matching gcc-4.0 byte-for-byte. `otool -l` shows `__const` is in `__DATA` (sectname/segname pair); `otool -rv` shows two classic external relocations targeting `_isalpha` (one inside `__DATA,__const`, one inside `__data`); `DYLD_PRINT_BINDINGS` confirms dyld writes the libSystem VA into both. tests2 111/111; bootstrap fixpoint holds; abitest cc 48 + tcc 24; v0.2.64-funcptr-const demo updated to drop the v0.2.64-specific layout pinning (now does functional checking); all v0.2.12..v0.2.64 demos still green (lua, bzip2, cjson, sed with `[[:class:]]`, gzip, dylib-slide, alacarte, gdebug-extern, gdebug-link, v0.2.63-extern-data-ref). Demo: [`v0.2.65-rodata-data-const.sh`](../demos/v0.2.65-rodata-data-const.sh). |
| [v0.2.64-g3](sessions/087-stt-func-roundtrip-2026-05-15/README.md) | 111 / 111 | **`.o`-roundtrip STT_FUNC reconstruction — function-pointer tables in `__TEXT,__const` finally work; sed's POSIX character classes go from SIGSEGV to working.** Pre-v0.2.64, `static const fn_t arr[] = { extern_func, ... }` SIGSEGV'd when called through. Three problems compounded: (1) Mach-O nlist has no `STT_FUNC` bit, so when tcc compiles `foo.c → foo.o` and the `.o` is read back at link time, the reconstructed undef sym is `STT_NOTYPE` — `collect_extern_stubs`'s `R_PPC_ADDR32 + STT_FUNC` clause (the v0.2.23 stub-allocation logic) fails to match and no stub gets allocated. (2) v0.2.63's writable-section gate excludes `__TEXT,__const` from extrel emission (dyld can't bind into a read-only segment). (3) With neither stub nor extrel, the writer falls back to writing the `__nl_symbol_ptr` slot's address into the table entry — but the slot holds the dyld-bound function VA as data, not as code, so calling through the entry jumps into data memory and faults. Canonical sufferer: sed's `dfa.c::prednames[]` (ctype function pointers in `__TEXT,__const`), flagged as a pre-existing bug by v0.2.63's verification. Fix: a single ~30-line pre-pass at the top of `collect_extern_stubs` paints `STT_FUNC` on every undef sym matching either of two heuristics — **(a)** any undef sym referenced via `R_PPC_REL24` is a function (`bl` is a function call); **(b)** any undef sym referenced via `R_PPC_ADDR32` from a `.rodata`-class section (which classify_section maps to `__TEXT,__const`) is treated as a function. The asymmetric trade-off is worth it: `static const fn_t arr[]` is vastly more common than `const T *const p = &extern_data`, and the latter was already broken pre-fix (slot-address fallback) so the new "still wrong but differently" (stub-address fallback) is no regression. Heuristic (b) is what catches sed, because Tiger's `<ctype.h>` macro-expands every `isalpha(c)` call to `__istype(c, _CTYPE_A)` — no direct `bl _isalpha` exists anywhere in the link for heuristic (a) to find, only the table entries' ADDR32s in `__const`. With STT_FUNC restored on the undef sym, `collect_extern_stubs`'s existing ADDR32+STT_FUNC clause allocates a stub, and `exe_resolve_section_relocs`'s `wants_extrel` predicate correctly evaluates false (the STT_FUNC check is already there from v0.2.63), so `exe_sym_addr` returns the stub VA — the table entry holds a callable PIC-free 16-byte stub that loads from `__nl_symbol_ptr` and branches to the dyld-resolved function. Pointer-equality between `&fn` and the table entry still doesn't hold (gcc-4.0 places `__const` with relocs in `__DATA,__const` and emits extrels; tcc keeps `.rodata` in `__TEXT,__const` and uses the stub trick — the addresses differ but calling through either works); the section-reclassification refactor required for full gcc parity is deferred. The type mutation is link-time-only — `classify_sym` continues to emit undef syms as `N_EXT|N_UNDF` so re-emitted `.o` files don't accumulate spurious FUNC bits. tests2 111/111; bootstrap fixpoint holds; **sed character-class regex** (`s/[[:alpha:]]/X/g`, `s/[[:digit:]]/D/g`, `s/[[:space:]]+/_/g`) goes from SIGSEGV (caller chain `dfa.c::dfaparse → dfacomp → dfawarn → compile_regex`) to PASS. All other demos green: lua/bzip2/cjson/gzip/dylib-slide/alacarte/gdebug-extern/gdebug-link/v0.2.63-extern-data-ref. v0.2.40's sed demo extended with three character-class assertions (each tagged "was SIGSEGV pre-v0.2.64"). Demo: [`v0.2.64-funcptr-const.sh`](../demos/v0.2.64-funcptr-const.sh) — compiles a program with a `static const` table of extern ctype function pointers via the `.o`-roundtrip path, inspects the linked exe to verify table entries point into `__TEXT,__symbol_stub` (not `__DATA,__nl_symbol_ptr`), and runs it. |
| [v0.2.63-g3](sessions/086-extern-data-ref-2026-05-15/README.md) | 111 / 111 | **Extern *data* references in static init now resolve to the dyld-bound target address — closes the long-deferred caveat from session 044.** Pre-v0.2.63, `int *p = &optind;` (where `optind` is `extern int` from libSystem) produced `p = &__nl_symbol_ptr[optind]` (the indirect pointer slot's address) rather than `p = &optind` (the libSystem int's address). Function-pointer ADDR32s already got their fix in v0.2.23 by allocating a 4-instruction stub and baking the stub address (closes the sqlite `aSyscall[fcntl]` case); the data variant had no parallel mechanism because the exe writer's `__LINKEDIT` had `extreloff = 0, nextrel = 0` — no externs ever flowed through dyld's binding path. v0.2.63 emits a classic Mach-O `struct relocation_info` (8 bytes; `r_extern=1, r_length=2 (long), r_type=PPC_RELOC_VANILLA, r_pcrel=0`) for each `R_PPC_ADDR32` against an undef *non-STT_FUNC* symbol whose containing section is writable (data / init_array / fini_array). LC_DYSYMTAB's `extreloff` / `nextrel` get populated, the `elfsym_idx → machsym_idx` mapping is resolved during nlist construction (extends the existing `elfsym_to_undef[]` table that already feeds stubs and nlptrs). Tiger's dyld treats VANILLA externs as `*location += symbolAddress` (ADD, not OVERWRITE), so nonzero in-place addends survive — verified by `int *q = &optind + 3` test case where the linker bakes `3 * sizeof(int) = 12` into the slot and dyld adds `&optind` to it at load time. Read-only sections (`__text` / `__const` / `__eh_frame` / DWARF) are explicitly excluded from extrel emission: dyld would SIGBUS trying to write into a non-writable segment. sed's `dfa.c::prednames[]` table of ctype function pointers in `__TEXT,__const` exposed this — Mach-O nlist has no STT_FUNC bit, so .o roundtrip silently drops the function-ness of an undef sym; the subsequent ADDR32 ref in `__const` would have looked like data to my new code and produced a load-time crash. Gating extrel emission on writable sections restores the pre-v0.2.63 slot-address fallback for these — still wrong but not regressing (sed's `--version` works because the table is never indexed by `--version`; same as pre-fix). The .o-roundtrip-STT_FUNC-loss problem is a separate issue tracked for future work. The dylib writer keeps the legacy slot-address fallback for now (dyld bind-info for dylibs needs additional load-command plumbing; deferred). tests2 111/111, bootstrap fixpoint holds; sed/lua/bzip2/cjson/gzip/dylib/alacarte/gdebug-extern/gdebug-link demos all PASS. Demo: [`v0.2.63-extern-data-ref.sh`](../demos/v0.2.63-extern-data-ref.sh) — compiles a program with two extern-data static-init pointers (zero-addend and +12-addend) against libSystem's `_optind`, asserts the linked exe has `nextrel = 2` in LC_DYSYMTAB, runs the binary, and checks both pointers resolve correctly to `&optind` and `&optind + 12`. |
| [v0.2.62-g3](sessions/083-crt1-stub-slot-investigation-2026-05-15/README.md) | 111 / 111 | **`PPC_RELOC_JBSR` non-extern resolution fixed — answers the "is it a bug or is dyld papering over it?" question raised by v0.2.61's diagnostic.** On its first real-world link (hello-world against `/usr/lib/crt1.o`), v0.2.61's `-vv` reader diagnostic surfaced four "non-extern stub-slot resolve failed" lines: the loader was silently dropping every `JBSR` reloc whose target lived in a section tcc had discarded (Apple's `__symbol_stub1`). The runtime worked anyway by lucky alignment of side-effects (the local thunks the JBSR jumps to had their own HI16/LO16 relocs against `__symbol_stub1` that resolved correctly through the indirect symtab, and tcc's writer happened to synthesize a parallel stub for the same extern via that thunk's anchor — so the BR24's dead branch into a live thunk reached tcc's freshly-emitted stub by coincidence), but the routing was fragile and any reordering of stubs would break it. Root cause: `macho_translate_relocs` was treating `PPC_RELOC_JBSR` identically to `PPC_RELOC_BR24` — reading the BR24 displacement out of the instruction and treating it as the target. For `BR24` that's correct (the displacement IS the target). For `JBSR` (Mach-O's long-branch convention) the BR24 immediate points at a 5-instruction **local trampoline** (`lis r12, hi; ori r12, r12, lo; mtctr r12; bctr`) that the assembler emitted because the real far-call target was outside 24-bit range; the actual target VA is carried in the **PAIR record's full 32-bit `r_address` field** (`otool` prints this as `other_part = 0x000003b0`, distinct from HA16/LO16/HI16 PAIRs which print `half = 0x03b0` because only the low 16 bits matter to those). Fix is a single split in the non-extern reloc dispatch: BR24 keeps the existing instruction-displacement path; JBSR reads `pair_w0 = mach_get32(r + 8)` (the PAIR following the JBSR) and uses that as `target_value`. Post-fix, the four crt1.o JBSRs resolve cleanly through `macho_resolve_stub_slot`: `(0x3b0 - 0x3b0)/16 = 0 → indirect[0] = _exit`, `(0x3d0 - 0x3b0)/16 = 2 → indirect[2] = _main`, `(0x3e0 - 0x3b0)/16 = 3 → indirect[3] = _atexit`, `(0x3c0 - 0x3b0)/16 = 1 → indirect[1] = ___keymgr_dwarf2_register_sections`. The existing post-resolution path (line 7316) already tags `S_SYMBOL_STUBS`-routed externs with `ST_PPC_NEEDS_STUB` and the existing `PPC_RELOC_JBSR → R_PPC_REL24` elf mapping (line 7375) emits the right reloc shape. Net: 4 silent reloc drops disappear, hello-world's `-vv` output shrinks from 17 reader lines to 13, the JBSR-routed externs are now explicit in tcc's symtab, and the existing PLT-stub machinery handles them through the documented code path. tests2 111/111, abitest cc+tcc 48/48, bootstrap fixpoint holds; all ten v0.2.25..v0.2.61 demos still PASS (the v0.2.61 demo's assertions are loose enough — count ≥ 5, both categories present — that the dropped lines don't fail it). No new demo: the v0.2.61 demo + a hello-world link is the observable behavior change. |
| [v0.2.61-g3](sessions/082-vv-reader-diagnostic-2026-05-15/README.md) | 111 / 111 | **Reader-side `-vv` diagnostic — symmetric counterpart to v0.2.50 / v0.2.59 / v0.2.60.** Those three bracket the writer (pre-write covers in-memory state, post-write covers on-disk bytes); v0.2.61 surfaces decisions on the *read* path. `macho_load_object_file` and its helpers (`macho_translate_relocs`, `macho_translate_sym`) make silent routing decisions in older releases — pass-1 section skips for Apple-internal stub / la_ptr / nl_ptr / debug sections, pass-3 wholesale reloc drops when a section was skipped, per-reloc drops for SECTDIFF targets that resolve nowhere or stub slots that fail, per-symbol filters for malformed nlist entries. Two motivating historical bugs would have been faster to triage with this diagnostic: v0.2.40 sed-bug (silent pass-1 section skip) and v0.2.44 SECTDIFF shadow-section bug (SECTDIFF target VA mapping to no loaded section). v0.2.61 adds a single ~30-line static helper `macho_reader_log` near the top of the reader section in `tcc/ppc-macho.c`, gated on `s1->verbose >= 2` (the existing `-vv` counter, already parsed by `do ++s->verbose; while (*optarg++ == 'v');` at `libtcc.c:2124`). 21 emit sites cover every silent decision: 2 section-skip sites (pass-1 in `macho_load_object_file` + the `macho_section_to_elf` return-0 cascade), 13 reloc-drop sites (1 in pass-3 of `macho_load_object_file` for wholesale-skipped-section drops + 12 inside `macho_translate_relocs` — SECTDIFF illegal for output type, unknown scattered type, SECTDIFF target VA maps to no section, SECTDIFF stub-slot resolve fail, SECTDIFF translated sym is 0, non-extern target section OOR, instruction at file offset past file end, non-extern reloc type/length unhandled, non-extern stub-slot resolve fail, extern target sym OOR, extern translate_sym filtered, extern r_type has no ELF mapping), 7 sym-skip sites (4 in `macho_translate_sym` — bad n_strx, empty name, n_sect OOR, unknown N_TYPE basic — and 3 in pass-2 of `macho_load_object_file` — local with non-N_SECT type, n_sect OOR, local in discarded section). STAB symbols (would flood under `-g` — debug syms are routinely skipped, not a routing decision) and standalone `PPC_RELOC_PAIR` continues (iteration mechanics — the paired half was consumed by the preceding iteration) are intentionally not logged. Output goes to **stderr** in `tcc: reader: <basename>: <category>: <details>` format, so it's separable from the writer's level-1 stdout prints (`<- /tmp/x (exe 8641 bytes, ...)`) and from `tcc -run` program output. The helper looks up `s1->current_filename` (set in `tcc_add_binary` at `libtcc.c:1129` right before the reader is called) and strips the directory prefix. Categories: `section-skip`, `reloc-drop`, `sym-skip`. Zero overhead at default verbosity — the helper short-circuits at the level check before any work. On a real link of a hello-world program against `crt1.o`, `-vv` surfaces 12 lines including the unconditional `__symbol_stub` / `__picsymbol_stub` / `__symbol_stub1` / `__nl_symbol_ptr` / `__la_symbol_ptr` section skips from crt1, four "non-extern stub-slot resolve failed" entries from crt1's internal calls into its own stub section (target VAs 0x330, 0x340, 0x350, 0x370 — three legitimate stub-slot boundaries that the loader's stub-slot resolver nonetheless rejects because crt1's `__symbol_stub1` has a high `sec->addr` base outside the indirect-table range computed by `resolve_stub_slot`; the calls work anyway because they go through dyld at runtime, not through tcc's translated symtab), and the two wholesale "section ... skipped: N relocs dropped wholesale" entries for the skipped stub sections that still had populated reloc tables. tests2 111/111, abitest cc+tcc 48/48, bootstrap fixpoint holds; all v0.2.25..v0.2.60 demos (10 from `v0.2.25-dylib` through `v0.2.60-post-write-linter`) still PASS. Demo: [`v0.2.61-vv-reader-diagnostic.sh`](../demos/v0.2.61-vv-reader-diagnostic.sh) — builds `foo.o` + `bar.o`, links them three times (default / `-v` / `-vv`); asserts default and `-v` stderr have zero `tcc: reader:` lines, `-vv` stderr has >= 5 reader lines covering both `section-skip` and `reloc-drop` categories with the documented format, `-g` + `-vv` produces zero `sym-skip` lines (STAB filter holds), and the linked program still runs correctly. |
| [v0.2.60-g3](sessions/081-post-write-linter-2026-05-15/README.md) | 111 / 111 | **Post-write linter — emission-side complement to v0.2.50 (EXE) and v0.2.59 (dylib) pre-write blocks.** Pre-write checks operate on writer state in memory, before any bytes are emitted; they can't catch bugs that happen during emission (`obuf` corruption, byteorder regressions where `put32be` slips to `put32le`, cmdsize literal/actual drift, truncated fwrites). v0.2.60 adds `macho_post_write_lint` (a single new ~470-line helper near the top of `tcc/ppc-macho.c`, called from both writers between `chmod(filename, 0755)` and `ret = 0`). It runs after fwrite+fclose, stats the file, reads it back through a fresh `open`/`read` path that does NOT share state with the writer, walks the Mach header + load commands + sections, and verifies: magic/cputype/cpusubtype/filetype as expected, sum-of-cmdsizes == header.sizeofcmds, walked count == header.ncmds, each LC_SEGMENT's cmdsize == 56+nsects*68, each segment's file range inside the file, each section's file/vm range inside its owning segment (file-only segments with `vmsize==0` such as `__DWARF` handled correctly), LC_SYMTAB / LC_DYSYMTAB cmdsize + exactly-one presence + partition arithmetic (`ilocalsym+nlocalsym == iextdefsym`, `iextdefsym+nextdefsym == iundefsym`, `iundefsym+nundefsym == nsyms`), nlist + strtab + indirectsym + extreloff + locreloff ranges all fit in the file, LC_LOAD_DYLIB / LC_LOAD_DYLINKER / LC_ID_DYLIB / LC_UNIXTHREAD counts match the writer-derived expected values (caller passes `is_dylib`, `expected_size`, `has_externs`, `n_extra_dylibs`; helper computes the rest). Same `tcc_error_noabort` + `goto cleanup` failure pattern as the pre-write blocks so every violation prints before the link aborts. Three hand-injections verified end-to-end on imacg3: (a) `put32be(&out, MH_MAGIC)` → `put32be(&out, 0xcafebabe)` in the EXE writer's Mach header — fires `ppc-macho: post-write: bad magic 0xcafebabe != MH_MAGIC 0xfeedface (byteorder regression?)`; (b) `put32be(&out, symtab_cmd_size)` → `put32be(&out, 999)` in the EXE writer's LC_SYMTAB — cascade of consistent diagnostics (`bogus cmdsize 999 (must be >=8 and 4-aligned)`, `walked 6 commands but header.ncmds = 10`, `sum of cmdsizes 848 != header.sizeofcmds 1152`, plus the LC_SYMTAB/LC_DYSYMTAB/LC_UNIXTHREAD missing-count diagnostics that follow); (c) same trick on the dylib writer's LC_DYSYMTAB cmdsize — confirms the dylib call site fires independently. Costs negligible (one `stat` + one `read` of an already-buffered file + a linear walk of < 1000 bytes of load commands); no behavior change on the happy path. tests2 111/111, abitest cc+tcc 48/48, bootstrap fixpoint holds; all eight existing dylib demos PASS. Demo: [`v0.2.60-post-write-linter.sh`](../demos/v0.2.60-post-write-linter.sh) — builds an EXE with externs (`printf`) and a dylib with externs (`printf` inside `sum3`), `dlopen`s the dylib through a driver, exercises both writer call sites on the happy path. |
| [v0.2.59-g3](sessions/080-dylib-self-link-diagnostics-2026-05-15/README.md) | 111 / 111 | **Self-link diagnostics ported to the dylib writer (roadmap #7, dylib half).** v0.2.50 added four pre-write sanity-check invariants to `macho_output_exe` that turned cryptic dyld errors into tcc-level diagnostics; the parallel `macho_output_dylib` writer never got the same treatment, so a dylib gone wrong (LC_DYSYMTAB miscounting, stub/slot mispairing, common-symbol-in-missing-bss, PIC layout drift) surfaced only as `dlopen("…") returned NULL` with no useful context. v0.2.59 mirrors the four checks into `macho_output_dylib`, between the `Compute __LINKEDIT layout` block and `Serialize: Mach header`. Adapted for the dylib writer's fixed points: (a) base = `DYLIB_TEXT_VMADDR_BASE` (0x40000000) instead of `EXE_TEXT_VMADDR_BASE`; no `__PAGEZERO`; no `__DWARF` segment in the dylib path (dwarf-sandwich check dropped); `text_seg_vmsize == text_seg_filesize` always for dylibs; (b) `__mh_dylib_header` instead of `__mh_execute_header`, no `entry_addr` check (dylibs have no LC_UNIXTHREAD); (c) PIC stubs are 32 bytes (8 instructions), not the EXE's 16-byte absolute stubs — bounds check uses `32u`; (d) section-presence check is identical. Each invariant verified by hand-injecting a deliberate break (`linkedit_vmaddr = 0xdeadbeef;` for (a); clobber `__mh_dylib_header.st_value` for (b); `stubs[0].addr = 0xdeadbeef;` for (c); `nsec = 0;` for (d)) and observing the resulting `tcc: error: ppc-macho: dylib …` message; happy-path overhead negligible (linear walks of already-computed layout data). tests2 111/111, abitest cc+tcc 48/48, bootstrap fixpoint holds; eight existing dylib demos (v0.2.25, v0.2.26, v0.2.29, v0.2.31, v0.2.40, v0.2.41, v0.2.42; v0.2.28 skipped — sqlite source not present) all PASS. Demo: [`v0.2.59-dylib-self-link-diagnostics.sh`](../demos/v0.2.59-dylib-self-link-diagnostics.sh) — dylib source touches every section class so all four checks walk every code path on a normal compile. |
| [v0.2.58-g3](sessions/079-dsymutil-debug-info-2026-05-14/README.md) | 111 / 111 | **Source-line debugging of a stripped exe via its `.dSYM` now works on Tiger** (closes roadmap #7.5 Phase 2 item 1b). Pre-v0.2.58 (v0.2.57 baseline), `dsymutil exe -o exe.dSYM` on a tcc-built `-gdwarf-2` exe produced a `.dSYM` whose `.debug_info` / `.debug_abbrev` / `.debug_line` / `.debug_str` `dwarfdump` reported as `< EMPTY >` (only `.debug_pubnames` was populated). `gdb info functions main` / `break *main` worked but `gdb list main` / `break <file>:<line>` failed with "No line number known for main" — a real source-line debugging gap. Root cause turned out to be a **layout** bug, not a content one. tcc compiles a tiny keymgr stub (the dyld-init-order fix from session 023) at link time via `tcc_compile_string`; that compile, under `-gdwarf-2`, writes a `<string>` compile-unit into the linker's `.debug_*` sections. `macho_output_exe` then bakes those into a small `__DWARF` segment on the linked exe. When `dsymutil` ran, it **preserved** the exe's `__DWARF` (load command order: before its own consolidated one) AND **appended** a second `__DWARF` segment with the consolidated content from the OSO chain. `dwarfdump` and Tiger `gdb 6.3` iterate segments in load-command order, find the FIRST `__DWARF` they see (the small keymgr one), report its empty-for-user-CUs `.debug_info` as `< EMPTY >`, and never look at the second segment. Fix is one-line in `tccelf.c`: save and clear `s->do_debug` around the keymgr-stub `tcc_compile_string` call so it doesn't emit a CU. The loader already drops `__DWARF,__debug_*` sections from loaded `.o` files (debug info flows via the OSO chain, see v0.2.56), so with the keymgr stub silenced too, the linker's `.debug_*` sections stay empty in a two-step build, and `macho_output_exe`'s `s->data_offset == 0` filter drops the `__DWARF` segment entirely — gcc-4.0 parity. `dsymutil` then produces a single-`__DWARF` `.dSYM`, and gdb / dwarfdump read the consolidated content. Single-step builds (`tcc -gdwarf-2 hello.c -o exe`) still ship the user CU embedded in the exe (the in-process compile writes directly into the linker's `.debug_*` sections, no `.o` intermediary), so `dwarfdump exe` keeps working for quick tests; the dsymutil flow is two-step territory. v0.2.57 demo updated to accept either layout (no `__DWARF` in exe is the new norm). tests2 111/111, abitest cc+tcc 48/48, bootstrap fixpoint holds; all v0.2.37–v0.2.57 debug demos green. Verified by [`v0.2.58-dsym-source-lines.sh`](../demos/v0.2.58-dsym-source-lines.sh) — asserts no `__DWARF` segment in the linked exe, exactly one `__DWARF` segment in the `.dSYM`, `dwarfdump` reads `TAG_compile_unit` from the `.dSYM`'s `.debug_info`, both source files appear as `AT_name` (one CU per `.o`), `strip -S` works, the stripped exe runs, and scripted gdb does `list main` / `list helper_add` / `break <file>:<line>` from the `.dSYM`. |
| [v0.2.57-g3](sessions/078-strip-and-debug-polish-2026-05-13/README.md) | 111 / 111 | **`strip -S exe` works on `tcc -gdwarf-2` Mach-O exes; `LC_UUID` emitted so Tiger gdb pairs the stripped exe with its companion `.dSYM`.** All in `macho_output_exe`: (1) `__DWARF` segment file-laid before `__LINKEDIT` (was: after) and page-padded so `__LINKEDIT.fileoff` lands on a page boundary — closes the cctools strip "the __LINKEDIT segment does not cover the end of the file (can't be processed)" abort, and avoids dyld's `doBindIndirectSymbolPointers` fault on a non-page-aligned `__LINKEDIT` mmap. VM layout unchanged (`__DWARF` keeps `vmaddr=0`, so `__LINKEDIT.vmaddr` still sits right after `__TEXT+__DATA`). (2) `__LINKEDIT` interior order swapped to gcc-4.0's canonical `symtab → indirect → strtab` (was `indirect → symtab → strtab`) — Tiger strip enforces it via the "symbol table out of place" assertion. (3) `LC_UUID` (24 bytes total) always emitted between `LC_DYSYMTAB` and `LC_UNIXTHREAD`; the 16 UUID bytes are two FNV-1a 64-bit hashes (different seeds, second pass folds the buffer in reverse) over the post-relocation bytes of `text + rodata + eh_frame + data + init_array + fini_array` and every `__DWARF` section, with RFC 4122 v4 marker bits set. Same source + toolchain → same UUID across rebuilds. dsymutil propagates the UUID into the `.dSYM` and Tiger gdb 6.3 pairs them automatically. (4) Per-function debug-map bracket gained an `N_SOL` record between `N_FUN(name)` and `N_FUN(size)` (gcc-4.0 chain-format parity). Pre-existing limitation NOT closed by this release: dsymutil's full `.debug_info` consolidation still produces an empty `.dSYM` (only `.debug_pubnames` populates), so post-strip gdb resolves symbol names via the `.dSYM` but not source-line breakpoints — investigation deferred to v0.2.58. tests2 111/111, abitest cc+tcc 48/48, bootstrap fixpoint holds; all v0.2.37–v0.2.56 debug demos still pass. Verified by [`v0.2.57-strip-uuid-dsym.sh`](../demos/v0.2.57-strip-uuid-dsym.sh) (LC ordering + `__LINKEDIT` end-of-file + non-zero deterministic `LC_UUID` + dsymutil UUID round-trip + N_SOL count + `strip -S` exit/run/UUID-survival + scripted gdb `info functions main` from `.dSYM`). |
| [v0.2.56-g3](sessions/077-n-oso-dsymutil-2026-05-13/README.md) | 111 / 111 | **`dsymutil-on-Tiger` consumes tcc-built `-gdwarf-2` Mach-O exes (roadmap #7.5 Phase 2 item 1b).** Three coupled changes. In `ppc-macho.c`: per-loaded-.o OSO state captured at `.o` load time (realpath via `realpath()`, mtime via `stat()`, `__TEXT,__text` offset + size, plus each defined-in-text function's name + offset from the `.o`'s nlist), gated to DWARF mode only — emitting in stabs mode would let Tiger gdb's OSO follower try to re-read stabs from tcc's `__DWARF,__stab` placement (which differs from gcc-4.0's `LC_SYMTAB` nlist placement) and SIGBUS. `emit_stab_nlist` now synthesizes a freestanding `N_SO source / N_OSO objpath / {N_BNSYM N_FUN(name) N_FUN("" w/size) N_ENSYM} per function / N_SO ""` chain in the linked exe's `LC_SYMTAB` per loaded `.o`, matching gcc-4.0's exact `-gdwarf-2` debug-map layout (function sizes computed by adjacency over each `.o`'s defined-in-`__text` symbols). In `tccdbg.c`, two PPC-narrow DWARF-2-strictness fixes: (1) `AT_frame_base` emits `DW_OP_reg31` (r31 = post-prolog FP set in `gfunc_prolog`) instead of `DW_OP_call_frame_cfa` — Tiger's `dwarf_utilities-42` doesn't recognize the DWARF-3 op and aborts with `failed assertion '!"Unknown one-operand"'`; (2) `AT_high_pc` (`DW_FORM_data4`) emits `low_pc + size` (absolute end-VA with a text-section reloc) instead of `size` (offset form, DWARF-3+ only) — pre-fix, the second-and-onward functions in a `.o` saw their `[low,high)` range collapse to empty and `dsymutil` silently dropped them. Plus: `__define_stab (N_OSO, 0x66, "OSO")` added to `tcc/stab.def`; lifecycle tracked on `TCCState` (`macho_oso_states` array plus per-state `funcs[]`, freed in `tcc_delete`). The stabs-mode flow (`tcc -g` post-v0.2.55 / explicit `-gstabs`) is unchanged — gdb reads stabs from `LC_SYMTAB` directly without going through the OSO chain. tests2 111/111, abitest cc+tcc 48/48, bootstrap fixpoint holds; all v0.2.37-v0.2.55 debug-related demos still pass. Verified by [`v0.2.56-n-oso-dsymutil.sh`](../demos/v0.2.56-n-oso-dsymutil.sh) (stabs build: zero OSO records, scripted gdb reads file:line directly; DWARF build: 6 SO/OSO entries with realpath+mtime, 3 BNSYM/ENSYM pairs, `dsymutil` exits 0 and the `.dSYM` bundle's `__debug_pubnames` lists `main` + `helper_add` + `helper_mul`). |
| [v0.2.55-g3](sessions/076-g-default-stabs-2026-05-13/README.md) | 111 / 111 | **Bare `-g` now defaults to stabs on PPC Darwin (roadmap #7.5 Phase 2 item 1d).** Pre-v0.2.55, plain `tcc -g foo.c -o foo` emitted DWARF-2 (set in v0.2.39 as the Tiger `dwarfdump`-friendly default); to get a `gdb 6.3`-debuggable binary the user had to explicitly type `-gstabs`. v0.2.55 flips the default: `confvars_set OSX dwarf=2` → `dwarf=0` in `tcc/configure`'s Darwin block, threading through `CONFIG_DWARF_VERSION` → `libtcc.c:2025`'s `s->dwarf = CONFIG_DWARF_VERSION` on bare `-g`. The rationale is that the stabs pipeline is now end-to-end robust across both the one-step `tcc -g foo.c` and the two-step `tcc -g -c foo.c; tcc -g foo.o` flows (v0.2.51 + v0.2.54 closed both), with stack offsets, body markers, and cross-TU debugging all working under Tiger gdb (v0.2.52, v0.2.53, v0.2.54); and `gdb 6.3` reads stabs natively from `LC_SYMTAB` while only partially understanding embedded DWARF in Mach-O. After this flip, `tcc -g hello.c -o hello && gdb hello` Just Works on Tiger out of the box. DWARF is still reachable via explicit `-gdwarf-2` / `-gdwarf-4` (Tiger `dwarfdump` keeps working unchanged), and the `-gdwarf`-without-version path still resolves to `DEFAULT_DWARF_VERSION = 2` (`tcc/tcc.h:1933`). One-line source change in `tcc/configure` plus a refreshed comment; tests2 111/111, abitest cc+tcc 24/24, bootstrap fixpoint holds, v0.2.54 cross-TU demo still green. Verified by [`v0.2.55-g-default-stabs.sh`](../demos/v0.2.55-g-default-stabs.sh) — bare-`-g` build's `__DWARF` segment has `__stab` but no `__debug_info`; explicit `-gdwarf-2` build has `__debug_info` but no stabs entries in `LC_SYMTAB`; gdb workflow on the bare-`-g` build prints params/locals/globals end-to-end. |
| [v0.2.54-g3](sessions/075-n-oso-scope-2026-05-13/README.md) | 111 / 111 | **`tcc -gstabs` stabs survive the `.o` round-trip (roadmap #7.5 prerequisite for Phase 2 item 1b).** Pre-v0.2.54, `tcc -gstabs -c foo.c -o foo.o` silently dropped the `.stab` / `.stabstr` sections at .o write — `classify_section` had no Mach-O mapping for them, so they fell out of the section table. The one-step `tcc -gstabs foo.c -o foo` flow was debuggable end-to-end (since stabs live in `s1->stab_section` throughout the compile-link); the two-step compile-then-link flow real Makefiles use was not. v0.2.54 closes that gap with four coupled changes in `ppc-macho.c`: (a) `classify_section` adds `.stab` → `__DWARF,__stab` and `.stabstr` → `__DWARF,__stabstr` mappings with `S_ATTR_DEBUG`, mirroring the existing `.debug_*` convention; (b) `macho_section_to_elf` adds the reverse mapping so the loader reattaches them to the merged `stab_section` + `.stabstr` when reading a tcc-built .o; (c) `emit_section_relocs` lets STT_SECTION-targeted relocs through to the local-handling path so the per-stab anchor relocs (against `text_section`'s section_sym) round-trip — they're not in the Mach-O nlist xlat table but the section number suffices; (d) a new Pass 1.5 in `macho_load_object_file` walks the just-loaded `.stab` entries and adds the merged-`.stabstr` offset to each non-zero `n_strx`, plus wires `stab_section->link` and `sh_entsize` on first encounter. Plus: a small bug shaken out in the SECTDIFF target-section search — once `__DWARF,__stab` is mapped, its `addr=0` overlaps the `__nl_symbol_ptr` slot range, so the search picked it incorrectly for PIC data references; the loader now skips `S_ATTR_DEBUG` sections during the target-section walk. And: `emit_stab_nlist` skips placeholder entries with `n_type == 0` so multi-`.o` round-trips don't emit junk stabs (each loaded `.o` contributes its own leading empty Stab_Sym before its real entries). The result: `tcc -gstabs -c main.c -o main.o; tcc -gstabs -c helpers.c -o helpers.o; tcc -gstabs main.o helpers.o -o exe` produces a Mach-O exe whose `LC_SYMTAB` carries the full cross-`.o` stab chain — Tiger gdb 6.3 sets breakpoints by file:line in both compilation units, prints params/locals/globals, and walks backtraces across the `.o` boundary. tests2 111/111, abitest-cc/tcc 24/24, bootstrap fixpoint holds; non-debug builds and `-gdwarf-2` are unchanged. Demo: [`v0.2.54-stabs-roundtrip.sh`](../demos/v0.2.54-stabs-roundtrip.sh) (two-`.o` build + 4 stab-section asserts + scripted gdb cross-TU walk). |
| [v0.2.53-g3](sessions/074-bnsym-ensym-2026-05-13/README.md) | 111 / 111 | **Apple-conventional `N_BNSYM` / `N_ENSYM` markers around every function body (roadmap #7.5, Phase 2 item 1c).** gcc-4.0 emits `N_BNSYM` (0x2e) and `N_ENSYM` (0x4e) stabs around each `N_FUN` entry to pin the function body's start/end addresses explicitly; tcc didn't, until v0.2.53. Implementation: two `put_stabs_r` calls in `tccdbg.c` (BNSYM in `tcc_debug_funcstart` before the opening N_FUN at `value=func_ind`; ENSYM in `tcc_debug_funcend` after the closing N_FUN size-entry at `value=func_ind+size`), both gated by `#ifdef TCC_TARGET_MACHO` and reloc'd against `text_section`/`section_sym` so the values resolve to the function's actual VAs through the existing `emit_stab_nlist` machinery. Both constants added to `tcc/stab.def` with the values from Apple's `<mach-o/stab.h>`. An A/B comparison against v0.2.52 — same source, both stabs builds, scripted gdb commands — found that **Tiger `gdb 6.3` itself shows no observable behavior change**: `break <fn>` lands at the same address (line 2, the body's first SLINE entry), `info line <fn>`, `info function <fn>`, and disassembly are byte-identical. The handoff anticipated a prolog-skip improvement (`break helper` landing a few instructions past entry without BNSYM/ENSYM); empirically `gdb 6.3`'s SLINE-based heuristic already finds the body's first source line cleanly on PPC. So 1c is convention/correctness work: tcc's stabs output now matches gcc-4.0's exact shape on the same target. **Real payoff is infrastructure for Phase 2 item 1b** — `dsymutil` walks the `BNSYM` / opening `N_FUN` / line stabs / closing `N_FUN` / `ENSYM` sequence when scoping a function's debug map; without these markers, `dsymutil exe -o exe.dSYM` (once `N_OSO` lands) would not see complete function boundaries. tests2 111/111, abitest-cc/tcc 24/24, bootstrap fixpoint holds. Verified by [`v0.2.53-bnsym-ensym.sh`](../demos/v0.2.53-bnsym-ensym.sh) (paired-marker assertions + v0.2.52 PSYM/LSYM regression check). |
| [v0.2.52-g3](sessions/073-gstabs-locals-2026-05-13/README.md) | 111 / 111 | **`print <param>` / `print <local>` now work under Tiger gdb (roadmap #7.5, Phase 2 item 1a).** v0.2.51 made `tcc -gstabs` produce gdb-debuggable Mach-O — file:line breakpoints, backtraces, and `print <global>` all worked — but `print <local>` / `print <param>` returned garbage from the function's own outgoing-param area because tcc's `N_PSYM` / `N_LSYM` `n_value` was r31 (FP) relative (`s->c = +24` for the first arg, `s->c = -12` and lower for locals), while Tiger gdb 6.3 on PPC interprets stabs offsets as r1 (post-prolog SP) relative — the gcc-4.0 convention. Since our prolog sets `r31 = r1 + frame_size`, the fix is one arithmetic shift at emit time: `tccdbg.c::tcc_debug_finish` (runs from `tcc_debug_funcend` immediately after `gfunc_epilog` has stashed `frame_size` in `ppc_last_frame_size`) now adds `ppc_last_frame_size` to every `N_PSYM` value and to every nonzero `N_LSYM` value before `put_stabs`. The nonzero guard exempts type-def LSYMs (struct fields / typedefs emitted by `tcc_get_debug_info` inside the function carry `value == 0`; real PPC param/local offsets are never zero — params start at +24, locals at -12). Gated by `#if defined(TCC_TARGET_PPC) && defined(TCC_TARGET_MACHO)`; DWARF untouched because its `DW_OP_fbreg <s->value>` against a frame-base of `DW_OP_call_frame_cfa` already resolves to `r1 + frame_size + s->value` via the v0.2.39 CFI. After the fix `print x` / `print y` / stepped locals (`next` → `print sum` → `next` → `print prod` …) and stack-walk frame values (`add3 (a=10, b=20, c=30)`, `main (argc=1, argv=0xbffffbe0)`) all work end-to-end. tests2 111/111, abitest-cc/tcc 24/24, bootstrap fixpoint holds. Verified by [`v0.2.52-gstabs-locals.sh`](../demos/v0.2.52-gstabs-locals.sh). |
| [v0.2.51-g3](sessions/072-oso-stab-gdb-tiger-2026-05-13/README.md) | 111 / 111 | **OSO STAB emission for gdb-on-Tiger (roadmap #7.5, Phase 1).** Tiger's bundled `gdb 6.3` reads Apple-format STAB entries directly from the linked exe's `LC_SYMTAB` nlist — no external .o files or `.dSYM` bundle required for `break <fn>` / `break <line>` / `list` / `bt` / `print <global>`. tcc's existing `-gstabs` mode already populated `stab_section` with the right Stab_Sym entries, but the Mach-O writer historically dropped them. v0.2.51 closes the gap: new `emit_stab_nlist()` in `ppc-macho.c` walks `stab_section` (and its reloc table) at link time and emits one nlist per Stab_Sym into the local-symbol range, translating `n_value` to runtime VAs via `sects[].vmaddr + tsym->st_value + addend` and `n_sect` to the 1-based Mach-O section ordinal. tccdbg.c switches `N_SLINE` / `N_LBRAC` / `N_RBRAC` emission to absolute form (`put_stabs_r` against `text_section` + `section_sym`) on `#ifdef TCC_TARGET_MACHO` since Apple's debug-map convention uses absolute VAs, not GNU function-relative offsets — plus a paired `N_FUN` end-entry (empty name, `n_value=size`) for unambiguous function boundaries. With this, `gdb hello-tcc` on Tiger sets file:line breakpoints, hits them, walks stacks with file:line per frame, and lists source. Phase 2 (parameter / local-variable offsets matching tcc's PPC frame layout, `N_OSO` chain for dsymutil round-tripping, defaulting `-g` to stabs on Darwin) is tracked in [session 072 HANDOFF](sessions/072-oso-stab-gdb-tiger-2026-05-13/HANDOFF.md). Verified by [`v0.2.51-gstabs-oso-stab.sh`](../demos/v0.2.51-gstabs-oso-stab.sh). tests2 111/111, abitest-cc/tcc 24/24, bootstrap fixpoint holds, non-debug + DWARF builds unchanged. |
| [v0.2.50-g3](sessions/068-self-link-diagnostics-2026-05-12/README.md) | 111 / 111 | **Self-link diagnostics (roadmap #7).** `macho_output_exe` runs four pre-write sanity checks that turn historically-cryptic dyld errors (`Cannot allocate memory`, `Symbol not found: __mh_execute_header`, SIGBUS in crt1) into tcc-level diagnostics with file/line/segment-aware context. Invariants: (a) VM layout (segments page-aligned, no overlap, `vmsize >= filesize`, `__LINKEDIT` placed after `__TEXT+__DATA` in both VA and file space — directly catches session-025's "Cannot allocate memory" failure); (b) `__mh_execute_header` registered as `N_ABS` at `__TEXT` base + `entry_addr` inside `__text`; (c) every stub VA inside `__symbol_stub1`, every `__nl_symbol_ptr` slot inside `__nl_symbol_ptr`, every `ST_PPC_NEEDS_STUB` symbol has a stub allocated — catches the session-025 crt1 SIGBUS; (d) every defined symbol's section is in the EXE writer's emitted section list — catches "common in `.bss` but `__bss` not emitted". Each check verified by hand-injecting a deliberate break and observing the resulting message; happy-path overhead <1 ms per link. Demo: [`v0.2.50-self-link-diagnostics.sh`](../demos/v0.2.50-self-link-diagnostics.sh). |
| [v0.2.0-g3](sessions/028-v0.2.0-g3-release/README.md) | 70 / 122 | full self-link, no gcc-4.0 in pipeline |
| [v0.2.1-g3](sessions/032-v0.2.1-release/README.md) | 75 / 122 | >8-arg fns, FP shadow for variadic |
| [v0.2.2-g3](sessions/033-v0.2.2-release/README.md) | 77 / 122 | more libgcc helpers, auto-link libtcc1.a |
| [v0.2.3-g3](sessions/035-unsupervised-2026-05-02/README.md) | 87 / 122 | struct-by-value parameters |
| [v0.2.4-g3](sessions/035-unsupervised-2026-05-02/README.md) | 96 / 122 | struct returns, dynamic param-area |
| [v0.2.5-g3](sessions/035-unsupervised-2026-05-02/README.md) | 101 / 122 | long-frame prolog, VLAs |
| [v0.2.6-g3](sessions/036-post-travel-2026-05-02/README.md) | 105 / 118 | constructor/destructor, VLA × callee-spill safety, BE-aware skips |
| [v0.2.7-g3](sessions/037-tcc-run-on-ppc-2026-05-02/README.md) | (104) | **`tcc -run` works on PPC for the first time** (PLT, @hi/@ha fix) |
| [v0.2.8-g3](sessions/038-real-ppc-atomics-2026-05-02/README.md) | 105 / 118 | real atomics via pthread_mutex |
| v0.2.9-g3 | 105 / 118 | minor cleanup |
| v0.2.10-g3 / v0.2.11-g3 | 109 / 111 | bcheck-style skip refinements; total drops 118 → 111 due to 6 bcheck-asserting tests properly skipped on PPC |
| [v0.2.12-g3](sessions/040-pickup-2026-05-03/README.md) | 109 / 111 | struct-deref-by-value + cross-TU PIC reloc translation. **Lua 5.4.7 builds + runs end-to-end.** |
| [v0.2.13-g3](sessions/040-pickup-2026-05-03/README.md) | 110 / 111 | BE Sym-union enum_val clobber fix; `scripts/bench.sh` (lua compile-time benchmark) |
| [v0.2.14-g3](sessions/041-pickup-2026-05-03/README.md) | **111 / 111 (100%)** | BE bitfield read-back fix (force byte-wise loads on PPC32 BE) |
| [v0.2.15-g3](sessions/041-pickup-2026-05-03/README.md) | 111 / 111 | Apple PPC ABI long-long alignment + LL field-load clobber. **sqlite3_open works.** |
| [v0.2.16-g3](sessions/041-pickup-2026-05-03/README.md) | 111 / 111 | LL-arg shuffle clobber in gfunc_call. **sqlite SELECT works; zlib full test suite passes.** |
| (post-v0.2.16, [session 042](sessions/042-upstream-tests-2026-05-05/README.md)) | 111 / 111 | First end-to-end upstream `tcc/tests/make test` run. 5 fixes: VT_STRUCT sym+off load, libtcc1.a missing helpers (`__builtin_*`, alloca, `__fixsfdi`), tcctest weak_test gating for Tiger dyld, ppc_pic_pairs leak, ship bcheck.o/bt-log.o stubs. tests2-dir under `-run` now 111/111 (was 108/111); memtest passes; test3 now actually runs (gets 770/4500 lines into tcctest before SEGV, was previously unable to even build the gcc reference). |
| [v0.2.17-g3](sessions/043-unsupervised-2026-05-05/README.md) | 111 / 111 | **alloca() actually works.** Fixed two coupled bugs: epilog used `addi r1, r1, frame_size` which can't recover from alloca having moved sp; and alloca's safety zone (between user region and outgoing param area) was 32 bytes, too small to absorb subsequent `printf` arg spills. Switched epilog to back-chain restore via FP, bumped alloca padding to 256 bytes. Also added more `__bound_*` stubs (strrchr, strstr, malloc family, etc.). Net upstream-test impact: full tcctest.c runs to completion (1020 lines vs 1012 reference, 255-line content diff that's all known long-double / `_Bool`-size / FPR-past-8 divergences); `abitest-tcc::ret_2float_test` now passes (was Bus error). |
| [v0.2.18-g3](sessions/043-unsupervised-2026-05-05/README.md) | 111 / 111 | **bzip2 1.0.8 works end-to-end.** Two more codegen fixes: (a) variadic FP arg past 8 FPRs had an off-by-one on TREG_TO_FPR (already 1-indexed, we double-added 1) so we stfd the wrong FPR — printf's 9th+ doubles were huge garbage. (b) float negation (`-x`) used the generic LE-only sign-flip (XOR 0x80 at offset size-1); on big-endian PPC the sign bit is at offset 0. Routed TOK_NEG through gen_opf for PPC, emitting real `fneg`. Net: tcctest.c diff vs reference shrinks 255 → 122 lines. bzip2 1.0.8 builds + round-trips on 573KB /etc/services with byte-identical decompress. |
| [v0.2.19-g3](sessions/043-unsupervised-2026-05-05/README.md) | 111 / 111 | Three more codegen fixes from chasing the tcctest diff: (a) FP `>=`/`<=` correctly return false for NaN (was firing the inverted-LT/GT bit, which is clear for NaN — added `cror cr0_LT, cr0_LT, cr0_FU` before bc). (b) pointer→int cast follows destination signedness (matches gcc; previously always sign-extended). (c) long-long arg straddling r10/stack (gslot=7, HIGH in r10, LOW on stack) writes BOTH halves; old `gv(RC_R(gslot))` only loaded one. Plus tcctest's LONG_DOUBLE = double on Apple PPC so gcc + tcc produce comparable output. tcctest.c diff: 122 → 44 lines. Remaining diff is all structural / known: aligntest3/4 alignof (Apple ABI), `_Bool` size (gcc-4.0 quirk), relocation_test addend (deferred), promote-char-funcret UB. |
| [v0.2.20-g3](sessions/043-unsupervised-2026-05-05/README.md) | 111 / 111 | **Apple PPC ABI 13-FPR support.** Bumped FP arg passing from 8 → 13 FPRs (NB_REGS 18 → 23, RC_F bitmask, reg_classes[], prolog spill area, fpr_index/fslot caps, all the related counts). Apple PPC32 actually uses f1..f13; we'd been capped at f1..f8 (the SysV/Linux convention). Functions with >8 FP args used to error "parameters exceed 8 FPR slots". After this, abitest::ret_8plus2double passes (had been failing since v0.2.0). Plus `__gcc_qadd / qsub / qmul / qdiv / __cmpdi2 / __ucmpdi2` stubs in lib-ppc.c so tcc-linked programs that include gcc-4.0-built objects don't hit "Symbol not found" at startup (Tiger libSystem doesn't ship the libgcc helpers). |
| [v0.2.21-g3](sessions/043-unsupervised-2026-05-05/README.md) | 111 / 111 | `__builtin_fabs / __builtin_fabsf / __builtin_inf / __builtin_inff` stubs. Tiger's `<math.h>` for PPC declares `fabs()` / `inf()` etc. via macros that expand to these gcc-style builtins. gcc inlines them; tcc emits regular function calls. Without stubs, programs that include `<math.h>` and use fabs/inf hit "Symbol not found" at startup. After this, **cJSON 1.7.18 builds + parses end-to-end** — fourth real-world program (lua, zlib, bzip2, cJSON). |
| [v0.2.22-g3](sessions/044-unsupervised-2026-05-05/README.md) | 111 / 111 | **`int *p = &arr[N]` (N != 0) static initializers actually point at `&arr[N]`.** Pre-v0.2.22 they collapsed to `&arr[0]` because `R_PPC_ADDR32` overwrote the in-place value (which is the implicit ELF Rel addend) instead of adding to it. Three small fixes — ppc-link.c::relocate() switches to `add` (matches i386's `R_386_32`); the Mach-O exe writer's resolver mirrors that; the Mach-O .o loader zeros the in-place after extracting the addend into the synthetic anchor sym. Drops `tcctest.c` diff vs gcc reference 44 → 33 lines (relocation_test divergence gone). Closes the highest-impact open item from session 043. |
| [v0.2.23-g3](sessions/044-unsupervised-2026-05-05/README.md) | 111 / 111 | **sqlite3 with on-disk database works end-to-end** — fifth real-world program (lua, zlib, bzip2, cJSON, sqlite). Static-init data slots like `(sqlite3_syscall_ptr)fcntl` used to resolve to the `__nl_symbol_ptr` slot ADDRESS (4 bytes of data, not callable code) — calling through them faulted. Fix: extend `collect_extern_stubs` to also allocate a stub when an extern STT_FUNC is data-referenced via `R_PPC_ADDR32` (the existing trigger was `R_PPC_REL24` calls only). The slot now holds a stub address; calling the stub jumps via `__nl_symbol_ptr` (filled by dyld lazily) to the real function. Closes both deferred sqlite bugs from session 041 (file-open SEGV; CREATE TABLE → SQLITE_CORRUPT — the latter was already fixed by v0.2.22's addend work, but never re-tested). |
| [v0.2.24-g3](sessions/044-unsupervised-2026-05-05/README.md) | 111 / 111 | **`tcc -ar` is now native Mach-O on PPC** (was: ELF parser only, fell back to system `/usr/bin/ar`). New code path inside `tcc_tool_ar()` handles fat (universal) binaries by locating the PPC32 slice, parses the Mach-O header + LC_SYMTAB, walks 12-byte nlist entries, and selects defined-external symbols (`(n_type & N_EXT) && (n_type & N_TYPE) == N_SECT`). Removes the `XAR=$(AR)` workaround in tcc/lib/Makefile so `libtcc1.a` builds via the in-tree tcc. Closes structural item #3 from the roadmap. tcc is now self-sufficient on Tiger PPC — no system `ar` needed in the build pipeline. |
| [v0.2.25-g3](sessions/045-unsupervised-dylib-2026-05-06/README.md) | 111 / 111 | **First Mach-O dylib output.** `tcc -shared -o foo.dylib foo.c` produces a real PPC Mach-O dynamic library on Tiger; dyld loads it via `dlopen`, dlsym finds exported symbols, function-call stubs survive dyld's slide. New `macho_output_dylib()` in `ppc-macho.c` (~600 lines, mirrors `macho_output_exe`): MH_DYLIB filetype; no `__PAGEZERO` / `LC_UNIXTHREAD` / crt-shim; `LC_ID_DYLIB` for self-name; `__mh_dylib_header` (vs `__mh_execute_header`); default `__TEXT` vmaddr 0x40000000. Function stubs are PIC (8 insns / 32 bytes per stub: `mflr/bcl/mflr/addis/mtlr/lwz/mtctr/bctr` with `(slot - .L1)` SECTDIFF anchor) — invariant under whole-image slide. `LC_DYSYMTAB` always emitted (dyld faults in `doBindExternalRelocations` without it, even with all-zero counts). Verified end-to-end on ibookg37: simple functions, libSystem calls (printf/strlen), static data, `__attribute__((constructor))`, pthread mutex, long-long arithmetic (libtcc1.a auto-linked). |
| [v0.2.26-g3](sessions/045-unsupervised-dylib-2026-05-06/README.md) | 111 / 111 | **Link-time dylib support: `tcc -lz hello.c` actually works.** `macho_load_dll()` (was a no-op stub) now parses the dylib's Mach-O header, walks `LC_SYMTAB` to register each defined-external symbol as UNDEF in our own symtab, captures the install name from `LC_ID_DYLIB`, and adds a `DLLReference` so the writer knows. Both `macho_output_exe` and `macho_output_dylib` now emit one `LC_LOAD_DYLIB` per loaded `DLLReference` (libSystem first, extras after). When extra (non-libSystem) dylibs are loaded, the writer switches to FLAT namespace (clears `MH_TWOLEVEL`) so dyld searches all loaded dylibs at runtime — avoids the per-symbol two-level-ordinal tracking that strict TWOLEVEL would require. **Closes upstream `dlltest`** (multi-session deferred): tcc compiles libtcc.c into libtcc2.dylib, links a tcc2 against that dylib, and tcc2 -run prints "Hello World" via the round-trip. Tested live with Tiger's bundled `/usr/lib/libz.1.dylib`. |
| [v0.2.27-g3](sessions/045-unsupervised-dylib-2026-05-06/README.md) | 111 / 111 | **JIT heisenbug fixed (deferred 5+ sessions).** When tcc compiles itself, `__TINYC__` is set and the inline-asm-using `__clear_cache(begin, end)` in `ppc-macho.c` falls through to a no-op stub. The JIT path (`tcc_relocate` → `protect_pages(..., 0/3)` → `__clear_cache`) therefore never actually flushed the icache. Reuse of the same JIT page across iterations (e.g. tcc_new/relocate/get_symbol/call/delete in a loop) saw stale instructions from icache, surfacing as SIGILL / SIGBUS / SIGSEGV / silent-wrong-result — the long-deferred "JIT heisenbug" that has been blocking abitest-tcc, libtest_mt, test3 and dlltest stability for 5 sessions. Fix: delegate to libSystem's `sys_icache_invalidate` (which performs the dcbst/sync/icbi/sync/isync sequence internally and is exported on Tiger). One-line change. test3 now runs to completion (was SEGV at line ~770/4500); abitest-tcc gets through 20 of ~24 sub-tests deterministically (was variable 5–19); dlltest is now stable. |
| [v0.2.49-g3](sessions/067-libtcc1-clz-ctz-2026-05-11/README.md) | 111 / 111 | **libtcc1.a `__builtin_clz` / `__builtin_ctz` UB values now match gcc-PPC (closes session 066's open item #4).** Pre-fix, tcc's libtcc1.a used a de-Bruijn-table fallback for `__builtin_clz` / `__builtin_clzl` / `__builtin_clzll` / `__builtin_ctz` / `__builtin_ctzl` / `__builtin_ctzll` that returned 31 / 31 / 63 / 0 / 0 / 0 on `x == 0`. gcc-4.0 on PPC inlines `cntlzw` for the 32-bit clz (so `clz(0) = 32`), derives `ctz(x) = 31 - cntlzw(x & -x)` (so `ctz(0) = -1`), and uses libgcc's `__ctzdi2` (`32 + ctz(lo)` when `lo == 0`, giving `ctzll(0) = 31`). The gcc spec marks these inputs as undefined behavior, so both implementations are technically conformant — but the divergence was a persistent csmith differential-testing nuisance, and session 062 papered over it with a `-include builtin_compat.h` UB-wrapper shim. Fix: one-line `if (!x) return XX;` short-circuit per function body in `tcc/lib/builtin.c`'s `BUILTIN(clz)` / `BUILTIN(clzll)` / `BUILTIN(ctz)` / `BUILTIN(ctzll)` (the `clzl` / `ctzl` aliases inherit through `__attribute__((alias))`). `clrsb` deliberately keeps the bare `CLZI` / `CLZL` macros — `clrsb(0) = 31` / `clrsbll(0) = 63` are well-defined per the gcc spec and the de-Bruijn path already returns those values. Verified by [`v0.2.49-clz-ctz-ub.sh`](../demos/v0.2.49-clz-ctz-ub.sh): 18 sub-tests across UB-inputs, defined-behavior smoke, and clrsb cases, all PASS. tests2 111/111, abitest cc+tcc all `success`, fixpoint holds, 7 release demos sampled. [Session 069](sessions/069-retire-clz-ctz-shim-2026-05-13/README.md) then retired the clz/ctz half of the `builtin_compat.h` shim: re-ran the `--builtins+bitfields` 8020-8419 sweep without it on imacg3 and got `PASS=352 FAIL=0 SKIP=48` — byte-identical seed-by-seed to session 066's `with-shim` baseline, confirming the wrappers were dead weight. The successor [`bswap_compat.h`](../scripts/csmith/bswap_compat.h) (promoted to [`scripts/csmith/`](../scripts/csmith/) in session 070) keeps only the bswap32/bswap64/ia32_crc32qi prototypes (separate ABI concern, gcc-4.0 lacks the builtins). |
| [v0.2.48-g3](sessions/066-seed-732-2026-05-11/README.md) | 111 / 111 | **r12 register-allocator clobber fix (closes seed-732 from session 065's 1000-seed csmith campaign).** Pre-fix, r12 was both in tcc's PPC int allocator pool AND hardcoded as scratch by `load()` / `store()` / `ppc_load_fp_const()` / `gfunc_call`'s struct-arg base. Under high register pressure the allocator parked a live vstack value in r12; the next `lis r12, ha(sym)` two-instruction global-load sequence silently destroyed it. Fix: one-line change in `ppc-gen.c` — `reg_classes[9] = 0` (was `RC_INT | RC_R(9)`). r12 is now true scratch; int pool drops from 10 slots (r3..r12) to 9 (r3..r11). The cost is one int register; for unoptimized code that's almost never tight. Session 066 re-ran session 065's 1000-seed default-opts campaign on ibookg37 against the post-fix tcc: **873 PASS / 0 FAIL / 127 SKIP**, clean (one better than the pre-fix 872 / 1 / 127). imacg3's `--builtins+bitfields` 8020-8419 sweep stayed at 352 / 0 / 48 byte-for-byte. tests2 111/111, abitest 24/24, fixpoint holds, all release demos pass. Two creduce attempts didn't shrink seed-732 usefully — the bug is sensitive to csmith's specific allocation order. Sibling r11 (slot 8) is in the same hardcoded-scratch / allocator-overlap situation in the rare large-stack-offset struct-arg path; csmith's small-program parameters don't surface it. If a future seed does, the symmetric fix is `reg_classes[8] = 0`. Verified by [`v0.2.48-r12-clobber.sh`](../demos/v0.2.48-r12-clobber.sh) — seed-732 vendored verbatim, asserts the end-of-run checksum is `76F5DB56` (gcc-4.0 -O0 reference). |
| [v0.2.47-g3](sessions/058-followups-2026-05-09/README.md) | 111 / 111 | **FP-arg GPR-shadow no longer clobbers a previously computed int arg.** Pre-fix, `gfunc_call`'s FP-arg variadic GPR-shadow code emitted `lwz r{gslot+3}, 16(r1)` (and double / straddle variants) directly to a hardcoded register without first calling `save_reg()`. If a previously-evaluated arg's value happened to live in that GPR, the shadow load clobbered it; arg setup later in pass 2 then emitted `mr r3, r{shadow}`, putting the float bit pattern in r3 where the int arg should have been. Surfaced via csmith `--float` seed 3228: `f((x &= helper(...)), 5.0f)` where `helper` returns into r3 and the `&=` result lands in r4, then arg2's float shadow targets r4 — printf in the callee saw `a=1084227584` (= 0x40A00000 = float 5.0 bit pattern) instead of 0. Fix: call `save_reg(gslot)` (and `gslot+1` for VT_DOUBLE both-in-GPR) before the shadow `lwz`. Mirrors the existing LL straddle / both-in-GPR paths. tests2 111/111, abitest-cc/tcc clean, bootstrap fixpoint holds, all 9 release demos pass; csmith C-float seeds 3228 + 3265 now agree with gcc-4.0 (3259 still diverges but that's a `(uint32_t)(negative_double)` UB conversion difference, consistent with our v0.2.46 fctiwz-semantics design choice). Verified by [`v0.2.47-fp-arg-shadow.sh`](../demos/v0.2.47-fp-arg-shadow.sh). |
| [v0.2.46-g3](sessions/056-csmith-2026-05-09/README.md) | 111 / 111 | **Float-to-int constant-fold matches PPC fctiwz semantics.** Pre-fix, tcc's compile-time const folder went through host `(int64_t)long_double` (= libgcc `__fix*di` on the build machine), which doesn't saturate to int32 range. So `int x = (int)1e10f` folded to `1410065408` (low 32 bits of `(int64_t)1e10`), while at runtime the same expression — emitted as `fctiwz` — gave `0x7FFFFFFF` (INT_MAX). Two tcc paths returned different answers for the same C source. Surfaced via csmith differential testing (`--float --builtins`, seed 92) — `func_65` had a chained `*(int*)p = (..., huge_float_const)` pattern where the const folder ran and disagreed with `g_99`'s gcc-4.0 reference. Fix: in tccgen.c's `gen_cast` constant-fold branch, on PPC target, emulate `fctiwz` for byte/short/int targets — saturate positive overflow to `0x7FFFFFFF`, negative overflow / NaN to `0x80000000`, otherwise round-toward-zero. VT_LLONG keeps the existing path because runtime there goes through `__fixsfdi` / `__fixdfdi` (libtcc1 lib-ppc.c) whose overflow semantics already match the host helper. tests2 111/111, abitest-cc/tcc 22/22, bootstrap fixpoint holds, all 9 demos pass. Verified by [`v0.2.46-float-int-const-fold.sh`](../demos/v0.2.46-float-int-const-fold.sh). |
| [v0.2.45-g3](sessions/055-bughunt-2026-05-09/README.md) | 111 / 111 | **BE PPC bitfield ABI fix — tcc now matches gcc / SysV PowerPC ABI.** Pre-fix, tcc on PPC laid out bitfields LSB-first within their storage container; the ABI uses MSB-first on big-endian. Internally consistent (struct field write+read round-trips), but ABI-incompatible: `union {uint32_t f0; unsigned f1:14;}; u.f0 = 0x024A1329; read u.f1` gave `0x0A02` (LSB-first byte-swap) instead of the standard `0x092` (top 14 bits). Pointer-cast access (`((union *)&plain_uint)->f1`) had the same bug. Surfaced via csmith differential testing (200 random C programs, gcc-4.0 as reference); seed 3 diverged at `g_55.f1` and reduced to a 16-line repro. Fix: BE branches in tccgen.c's three byte-wise bitfield paths (`load_packed_bf`, `store_packed_bf`, `init_putv` static initializer) interpret bit_pos as MSB-first within the container, walk bytes from MSB end (low memory address on BE) toward LSB end, and accumulate result bits MSB-first; struct_decl's bit_pos counter is unchanged. Bootstrap fixpoint, tests2 (111/111), abitest-cc (22/22), abitest-tcc (22/22), all real-world demos hold. After fix, csmith 200/200 pass with default options. Verified by [`v0.2.45-be-bitfield-abi.sh`](../demos/v0.2.45-be-bitfield-abi.sh). |
| [v0.2.44-g3](sessions/054-unsupervised-2026-05-09/README.md) | 111 / 111 | **`tcc -g` linking separately-compiled .o files no longer produces SEGV-ing executables.** After v0.2.43's smap NULL fix unblocked `tcc -g -c foo.c`, the next problem appeared at the link step: `tcc -g -o exe a.o b.o c.o` produced an exe that segfaulted at runtime when accessing a global pointer initialized to an extern symbol's address. Single-source compile-and-link worked. Root cause: in `macho_translate_relocs`, the SECTDIFF target-section lookup found the FIRST section whose `[addr, addr+size)` matched `sc_value`. Without -g, only the real sections (text/data/__nl_symbol_ptr/stub) had non-zero addresses; the lookup found the right section. With -g, the debug sections (`__debug_info`, `__debug_abbrev`, etc.) all start at addr=0 with large sizes; one would shadow the real `__nl_symbol_ptr` (e.g. addr 0x180 size 4), and the lookup returned the wrong section index. `macho_resolve_stub_slot` then computed a bogus indirect-table index, returned -1, and the SECTDIFF reloc was silently dropped — leaving the .o-internal displacement baked into the linked exe's addis/lwz immediates. Fix: in the section-search loop, only consider sections that can plausibly be SECTDIFF targets (real merged tcc sections, or stub-like sections of type S_SYMBOL_STUBS / S_LAZY_SYMBOL_POINTERS / S_NON_LAZY_SYMBOL_POINTERS); debug sections are skipped. Side effect: tcc-built Python 2.7.18 with -g enabled now works (the v0.2.42 demo can drop its `-g`-stripping workaround). Verified by [`v0.2.44-gdebug-link.sh`](../demos/v0.2.44-gdebug-link.sh): `*p` cross-TU and a 3-TU function-pointer-indirect-call test, both linked with -g, both produce the right output. |
| [v0.2.43-g3](sessions/054-unsupervised-2026-05-09/README.md) | 111 / 111 | **`tcc -g` no longer SIGBUSes on `extern int v; int *p = &v;`**. Pre-v0.2.43, compiling a static initializer that referenced an extern symbol with `-g` enabled tripped a NULL-pointer dereference inside `ppc-macho.c::classify_sym`'s smap-walking loop. The path: tccgen.c::init_putv emits a R_DATA_PTR reloc against the extern sym; put_extern_sym2 calls tcc_debug_extern_sym which emits a DWARF DW_TAG_variable entry; at OBJ write time, classify_sym walks `smap[]` looking for the section containing each defined symbol, but the synthesized __picsymbolstub1 / __la_symbol_ptr / __nl_symbol_ptr entries have `smap[j].elf == NULL` — derefing `smap[j].elf->sh_num` was the crash. Fix: NULL-check matching the four other identical loops in the same file. Surfaced trying to build Python 2.7.18 with `-g` (Modules/config.c). Verified by [`v0.2.43-gdebug-extern.sh`](../demos/v0.2.43-gdebug-extern.sh) — 3 patterns (extern data, extern function pointer, mixed struct array) compile cleanly with `-g`, all `dwarfdump`-clean. Note: a separate `-g` regression remains in the `.o`-then-link path — when -g is enabled and the source crosses TU boundaries, the linked exe ends up with PIC slot offsets pointing into __text instead of __nl_symbol_ptr (under investigation; tracked in session 054 README). |
| [v0.2.42-g3](sessions/054-unsupervised-2026-05-09/README.md) | 111 / 111 | **Python 2.7.18 builds + runs end-to-end with tcc** — eighth real-world program (after lua, zlib, bzip2, cJSON, sqlite, sed, gzip), and by far the largest at ~150 .c files / ~600k lines. No new tcc fixes — Python was previously dead at startup with `Fatal Python error: Can't initialize type type` (PyType_Ready), but v0.2.41's `extra_off`-truncation fix in the PIC load/store paths fixed it as a side effect: PyTypeObject is large (220+ bytes) and its members live well past the 16-bit-signed displacement range that pre-v0.2.41 tcc was masking. Verified by [`v0.2.42-python.sh`](../demos/v0.2.42-python.sh): runs a Python program that exercises list comprehensions, recursion, classes/methods, string ops, dict walks, exception handling, and floating-point formatting. (Demo strips `-g` from CFLAGS as a workaround for a newly-found tcc bug — `-g` SIGBUSes when emitting DWARF for a static initializer that references an extern symbol; minimal repro `extern int v; int *p = &v;`. Filed for next session.) |
| [v0.2.41-g3](sessions/054-unsupervised-2026-05-09/README.md) | 111 / 111 | **GNU gzip 1.11 round-trips at all sizes** — seventh real-world program. Pre-v0.2.41, tcc-built gzip produced corrupt output at >= 64 KB inputs (system `gunzip` rejected with CRC errors). Root cause: ppc-gen.c's PIC load/store paths used `(extra_off & 0xffff)` to compute the D-form immediate, truncating byte offsets that exceeded 16-bit signed range. Surfaced by gnulib's `#define head (prev+WSIZE)` pattern: `head[n]` becomes `*(prev + WSIZE + n)`, byte offset = `WSIZE * sizeof(Pos) = 0x10000` — masked to 0. fill_window's slide loop (triggered when `strstart >= 65274`) updated `prev[]` twice and never `head[]`, corrupting the hash chains. Fix: `ppc_adjust_extra_off` helper that emits `addis addr_gpr, addr_gpr, ha16(extra_off)` for offsets that don't fit, returning the lo16 portion. Applied at the top of all four affected PIC paths in `load()` and `store()` (extern data symbol load+store and !want_load address-of for sym+off). Verified by [`v0.2.41-gzip.sh`](../demos/v0.2.41-gzip.sh) — round-trips 1 KB through 500 KB inputs plus `/etc/services` through both tcc-built gzip self-decompression and system `/usr/bin/gunzip`. |
| [v0.2.40-g3](sessions/054-unsupervised-2026-05-09/README.md) | 111 / 111 | **GNU sed 4.8 builds + works end-to-end with tcc** — sixth real-world program (after lua, zlib, bzip2, cJSON, sqlite). Three substantive tcc fixes: (1) `_Bool` store via pointer (`*p = b;` for `_Bool *p`) and `_Bool` load-via-sym+off were unhandled in ppc-gen.c. (2) `__attribute__((gnu_inline))` recognized; predefine `__GNUC_GNU_INLINE__=1` on Apple. gnulib's c-ctype.h uses `_GL_INLINE` which on gcc-4.x defaults to gnu89-inline semantics ("extern inline" provides body for inlining only, with the strong def in c-ctype.c); without the attribute handling every TU emitting c-ctype.h's inlines tripped "defined twice" link errors. (3) PIC SECTDIFF reloc translation for forward-ref-then-tentative-def-of-global. When tcc parses regex.c it sees `extern reg_syntax_t re_syntax_options;` from regex.h first (PIC reloc emitted), then a tentative def at line 243. The .o writer baked the .o-internal SECTDIFF displacement into the addis/addi immediates; macho_translate_relocs was silently dropping the reloc translation when its target lived in a real merged section (BSS here), leaving the wrong constant in the linked exe. Result: re_compile_pattern read 0 for the syntax flag, ERE `(...)` / BRE `\{n,m\}` patterns silently failed to compile. Fix: when a SECTDIFF target is a non-discarded merged section, synthesize an anonymous local anchor sym at the target offset and emit R_PPC_*_PIC against it — the existing PIC-fallback resolver handles the rest. Verified by [`v0.2.40-sed.sh`](../demos/v0.2.40-sed.sh). |
| [v0.2.39-g3](sessions/053-dwarf-exe-2026-05-08/README.md) | 111 / 111 | **DWARF `__eh_frame` + per-prolog CFI in linked Mach-O exe; default to DWARF-2 on Darwin.** Three coupled changes: (a) tcc.h enables `TCC_EH_FRAME` for `TCC_TARGET_MACHO` (was excluded by `ELF_OBJ_ONLY`); tccelf.c keeps `unwind_tables` on for non-ELF outputs when `-g`; classify_section maps `.eh_frame` → `__TEXT,__eh_frame`; macho_output_exe lays out + relocates + emits eh_frame. (b) tccdbg.c PPC FDE block emits per-FDE CFI (`advance_loc 96; def_cfa_offset frame_size; offset_extended 65, (frame_size-8)/4`) — describing the post-prolog CFA shift across `stwu` and the LR save. ppc-gen.c stashes frame_size in `ppc_last_frame_size`. (c) Mid-section .eh_frame terminator strip in macho_output_exe: every input .o that tcc compiled with unwind_tables on contributes a trailing 4-byte zero record; the merger left them embedded mid-section, where dwarfdump and lldb both stop at the first one. Walks records, drops length=0, fixes up FDE CIE_pointers, re-appends a single clean terminator. Bonus: configure now defaults `-g` to DWARF-2 on Darwin (was 4) — Tiger's bundled tools fully understand DWARF-2 and only partially DWARF-4. ppc-link.c::gotplt_entry_type marks `R_PPC_REL32` as NO_GOTPLT_ENTRY (used by FDE PC-begin fields). Verified by [`v0.2.39-eh-frame.sh`](../demos/v0.2.39-eh-frame.sh). |
| [v0.2.38-g3](sessions/053-dwarf-exe-2026-05-08/README.md) | 111 / 111 | **DWARF debug info in linked Mach-O exe.** Closes the second half of the DWARF arc started in v0.2.37. macho_output_exe walks `s1->sections[]` for `S_ATTR_DEBUG` sections and lays them into a `__DWARF` segment placed after `__LINKEDIT` (vmaddr=0/vmsize=0; real file_offset/file_size; per-section headers carry S_ATTR_DEBUG). Cross-DWARF R_PPC_ADDR32 resolves to "0+addend=addend" (debug section vmaddr=0); DWARF→.text R_PPC_ADDR32 resolves to text_runtime_va + addend. Adds R_PPC_REL32 case to `exe_resolve_section_relocs` (used by `.eh_frame` FDEs) and fixes the matching addend-loss bug in ppc-link.c::relocate. dwarfdump on a tcc-linked exe reads compile unit / abbrev / line table / aranges / strings; AT_low_pc / AT_high_pc carry the actual runtime VAs. Tiger's gdb 6.3 doesn't read embedded `__DWARF` segments — it expects OSO STAB references or `.dSYM` bundles — but dwarfdump and lldb (later macOS) read this format directly. Verified by [`v0.2.38-dwarf-exe.sh`](../demos/v0.2.38-dwarf-exe.sh). |
| [v0.2.37-g3](sessions/052-dwarf-obj-2026-05-07/README.md) | 111 / 111 | **DWARF debug info in `-c` Mach-O output.** Pre-fix, `tcc -c -g foo.c` accepted -g but emitted no DWARF — `dwarfdump foo.o` printed `<EMPTY>`. v0.2.37 wires up the existing tccdbg.c machinery for PPC: tccdbg gains PPC-aware blocks (DWARF instr-len 4, return-address column 65 for LR, SP=r1, code-align 4 / data-align -4) and target-endian write helpers (PPC32 is BE; DWARF integers follow target endianness, so the bare `write32le` calls were producing little-endian DWARF that dwarfdump on a BE Mach-O couldn't parse). ppc-link.c learned R_PPC_REL32. ppc-macho.c's `classify_section` now accepts `.debug_*` sections, mapping each to `__DWARF,__debug_*` with S_ATTR_DEBUG; their `addr` is left at 0 so cross-section R_PPC_ADDR32 relocs resolve to "0 + addend = addend" (the linker / dsymutil reapplies on top with the real section base). Net: `dwarfdump foo.o` now produces standard DWARF dump (compile unit, AT_producer = "tcc N.N", types, line table). DWARF in the linked exe path is the natural follow-up — `macho_output_exe` doesn't yet enumerate DWARF sections; tracked in [docs/sessions/052/HANDOFF.md](sessions/052-dwarf-obj-2026-05-07/HANDOFF.md). Verified by [`v0.2.37-dwarf-obj.sh`](../demos/v0.2.37-dwarf-obj.sh). |
| [v0.2.36-g3](sessions/051-fmadd-2026-05-07/README.md) | 111 / 111 | **`__builtin_fma` / `__builtin_fmaf` emit PPC hardware fmadd inline.** Pre-v0.2.36, `__builtin_fma()` failed at runtime with "Symbol not found" because tcc treated it as an extern function and there was no library stub. v0.2.36 adds `TOK_builtin_fma` / `TOK_builtin_fmaf`, dispatches them in tccgen.c's expression parser, and emits PPC's `fmadd FRT,FRA,FRC,FRB` (or `fmadds` for single) directly — one instruction, fused (single rounding step). Side benefit: lib-ppc.c's `dd_two_prod` (used by all `__gcc_q*` arithmetic helpers from v0.2.34) collapses from a 10-flop Veltkamp+Dekker product to 2 flops (`p = a*b; e = fma(a, b, -p)`); all v0.2.34 dd-precision invariants still hold. Verified by [`v0.2.36-builtin-fma.sh`](../demos/v0.2.36-builtin-fma.sh) (checks the disasm contains `fmadd` and not `bl _fma`, then runs 4 algebraic invariants). |
| [v0.2.35-g3](sessions/050-libm-ldbl128-2026-05-07/README.md) | 111 / 111 | **libm LDBL128 redirect for math functions.** v0.2.32 wired the stdio.h `<sys/cdefs.h>` `__DARWIN_LDBL_COMPAT` redirect to point printf/scanf/etc. at the 16-byte-LD libSystem entries (via `__LDBL_MANT_DIG__=106` + `__ENVIRONMENT_MAC_OS_X_VERSION_MIN_REQUIRED__=1040`). The math.h analog (`<architecture/ppc/math.h>`'s `__LIBMLDBL_COMPAT`) was missed: math functions on `long double` (ldexpl, expl, sinl, cosl, fabsl, etc.) were silently going to the legacy 8-byte-LD libm entries that treat the LD as plain double. Most visibly, parse_number's `ldexpl(mantissa, exp_val - frac_bits)` was returning the input unscaled, so tcctest's `0x0.88p-1022` literal got parsed as 136 instead of ~1.18e-308 (the canonical "subnormal" sub-test). Fix: predefine `__LONG_DOUBLE_128__=1` so `__WANT_LONG_DOUBLE_FORMAT__` evaluates to 128 in the math.h block and the `__LIBMLDBL_COMPAT(sym)` macro emits `__asm("_" #sym "$LDBL128")`. tcc now references e.g. `_ldexpl$LDBL128`, the 16-byte-LD libSystem entry. Net: `make test3` content-diff drops from 45 → 13 lines (the 13 remaining are structural: Apple ABI alignof, gcc-4.0 _Bool size, UB casts). Verified by [`v0.2.35-libm-ldbl128.sh`](../demos/v0.2.35-libm-ldbl128.sh). |
| [v0.2.34-g3](sessions/049-dd-precision-2026-05-07/README.md) | 111 / 111 | **Precision-correct double-double arithmetic in libtcc1.a.** v0.2.32 made the LD ABI real, but the `__gcc_q{add,sub,mul,div}` and `__{eq,ne,lt,le,gt,ge}tf2` helpers in lib-ppc.c were lossy stubs (high-double only, low-half always zero). Programs whose long-double values exceeded double's 53-bit mantissa silently lost precision: `(2^53 + 1) - 2^53` returned 0 instead of 1. v0.2.34 replaces them with proper Knuth Two-Sum + Veltkamp split + Dekker product implementations (the standard libgcc soft-fp dd algorithms). No fma dependency — Veltkamp+Dekker uses regular multiplies; PPC's hardware `fmadd` would be faster but tcc-on-PPC doesn't emit it and we'd rather not pull in libm just for this. Comparisons now use `(hi == hi && lo == lo)` for equality and lexicographic compare on `(hi, lo)` for ordering — exact for canonical dd values (which everything our arithmetic produces is). Verified by [`v0.2.34-dd-precision.sh`](../demos/v0.2.34-dd-precision.sh) (5 invariants from the standard dd-test suite). Bootstrap fixpoint, tests2 (111/111), abitest-cc (24/24), abitest-tcc (24/24), libtest, dlltest still pass. |
| [v0.2.33-g3](sessions/048-libtest-rel24-2026-05-07/README.md) | 111 / 111 | **libtcc-callback (SHN_ABS) reachability via PLT trampolines.** Pre-v0.2.33, calls from JIT-compiled code into a callback registered with `tcc_add_symbol` failed when the JIT region landed >32MB from the libtcc binary (PPC `bl` has a 24-bit signed displacement). `build_got_entries` skipped synthesizing PLT trampolines for absolute (SHN_ABS) symbols on 32-bit platforms via a `PTR_SIZE != 8` early-`continue`; the comment said "on 64-bit platforms these need to go through .got" but the same constraint applies on PPC32 in practice. Fix: extend the bypass exception to include `TCC_TARGET_PPC` alongside `TCC_TARGET_ARM`. The existing PPC `create_plt_entry` / `relocate_plt` machinery already handles the trampoline emission (4-instruction `lis/ori/mtctr/bctr`); just needed to reach it. **Closes upstream `libtest`** (mistakenly listed as passing in session 046's HANDOFF — verified failing both before and after v0.2.32 by stash-and-retest). Verified by [`v0.2.33-libtcc-callback.sh`](../demos/v0.2.33-libtcc-callback.sh): a small libtcc-using program registers an `add(int,int)` callback, JIT-compiles a function that calls it, runs it, asserts the result. |
| [v0.2.32-g3](sessions/047-longdouble-2026-05-07/README.md) | 111 / 111 | **`long double` = IBM double-double on Apple PPC.** Pre-v0.2.32 tcc treated `long double` as 8-byte double (LDOUBLE_SIZE=8) which violated Apple PPC32's actual 16-byte IBM-double-double ABI. tcc-built code mixed with gcc-built code over the LD ABI silently corrupted values: `abitest-cc::ret_longdouble_test` and `arg_align_test` had been the last two abitest-cc failures since v0.2.0. v0.2.32 makes LD a true 2-FPR-pair value (`LDOUBLE_SIZE=16, LDOUBLE_ALIGN=4`; `R2_RET(VT_LDOUBLE) = REG_FRE2 = TREG_F(1)`); codegen routes LD `+ - * /` through `__gcc_q{add,sub,mul,div}` and LD compares through `__{eq,ne,lt,le,gt,ge}tf2`; LD args spill to the outgoing param area then lfd into ABI FPR pair (decouples regalloc-time from ABI placement); LD return = (f1, f2). Predefines `__LDBL_MANT_DIG__=106` and `__ENVIRONMENT_MAC_OS_X_VERSION_MIN_REQUIRED__=1040` so Tiger's `<sys/cdefs.h>` redirects `printf`/`scanf` to the `*$LDBL128` symbols (the 16-byte LD libSystem entries). lib-ppc.c grew long-double helpers (lossy double-fallback) plus LL↔LD conversion stubs. **abitest-cc now 24/24** (was 22/24). Bootstrap fixpoint, tests2 (111/111), abitest-tcc (24/24), dlltest, all real-world demos hold. |
| [v0.2.31-g3](sessions/046-dylib-slide-2026-05-06/README.md) | 111 / 111 | **Const-data-with-pointers slide fix.** v0.2.29's locrel emission covered `__data`/`__mod_init_func`/`__mod_term_func` but not `__const`. Pointer-bearing `static const struct { const char *name; ... } tbl[]` tables landed in `__TEXT,__const` which is read-only at runtime; dyld couldn't write the slide fixups to the embedded pointers, so under slide they stayed unslid and any dereference crashed (most visibly: tcc_compile_string in a slid libtcc2.dylib SIGBUS'd in `tcc_options[]`). Fix: in `tccgen.c` when `output_type == TCC_OUTPUT_DLL`, route initialized const data to `data_section` instead of `rodata_section`. Trade-off: lose the RO protection for const data in dylibs, but ensure correctness under slide. With this, libtcc2.dylib loads and `tcc_compile_string`/`tcc_relocate`/`tcc_get_symbol` all work end-to-end at a slid address. Verified with [`v0.2.31-libtcc-slide.sh`](../demos/v0.2.31-libtcc-slide.sh) (a 5-entry `{const char*, int}` lookup table that survives slide). |
| [v0.2.30-g3](sessions/046-dylib-slide-2026-05-06/README.md) | 111 / 111 | **Mach-O archive alacarte loader.** Pre-v0.2.30, tcc force-loaded every member of every `.a` archive on Mach-O. v0.2.30 parses the archive's BSD `__.SYMDEF SORTED` (or `__.SYMDEF`) symbol table, looks up each entry against the current undefined-symbol set, and pulls in only the members that actually resolve a reference; iterates until no new members get added. Also extends the SysV-style `/` symdef path (used by tcc -ar-built archives like `libtcc1.a`) to sniff each loaded member's magic and route Mach-O `.o` files through `macho_load_object_file`. Closes structural roadmap item #4. Verified with [`v0.2.30-alacarte.sh`](../demos/v0.2.30-alacarte.sh): a 3-member archive of which only 2 members are referenced; binary contains exactly the 2 referenced symbols and not the third. |
| [v0.2.29-g3](sessions/046-dylib-slide-2026-05-06/README.md) | 111 / 111 | **Dylib slide support.** Pre-v0.2.29, `tcc -shared` dylibs only worked when dyld loaded them at the preferred vmaddr (0x40000000). If anything else occupied that range, dyld slid the image and absolute refs (in `__data` initializers, in `__init_array` fn pointers, and in `lis/addi` pairs in `__text`) pointed at the unslid VAs — silently wrong-result or SIGBUS. Two changes: (a) `ppc_need_pic_for_sym` now returns 1 for locally-defined symbols in DLL mode, routing code refs through PIC sectdiff (sym − anchor); PIC base setup is also pinned to function prolog so r30 is valid before every PIC use regardless of control flow (lazy emission was unsafe — first use could be inside a conditional branch, leaving r30 stale on other paths in the same function). (b) `macho_output_dylib` walks reloc tables of `__data`, `__mod_init_func`, `__mod_term_func` and emits a Mach-O VANILLA local `relocation_info` for every R_PPC_ADDR32 against a locally-defined symbol; dyld walks LC_DYSYMTAB.locrel at load time and adds the slide. dlltest still passes; abitest-tcc still 24/24; tests2 still 111/111; bootstrap fixpoint holds. Verified with [`v0.2.29-dylib-slide.sh`](../demos/v0.2.29-dylib-slide.sh) — load at preferred vs. with `mmap(MAP_FIXED)` blocker, both produce identical results. |
| [v0.2.28-g3](sessions/045-unsupervised-dylib-2026-05-06/README.md) | 111 / 111 | **abitest-tcc 24/24 + PPC backtrace support.** Two more landed: (a) `gfunc_call` was using `save_regs(nb_args + 1)`, which skipped the func-ptr slot — for indirect calls where the function pointer is an LVAL with its address in a volatile GPR (e.g. `(*(s2->f2 = &f) + 0)(v,v,v,v,v,v,1.0)` in upstream `many_struct_test_3`), the arg-pass-2 code clobbered that register before the indirect-call site could `gv()` it. Switched to `save_regs(nb_args)` (matches x86_64-gen.c). many_struct_test_3 + stdarg_test + stdarg_many_test + stdarg_struct_test + arg_align_test all flip from crash/fail to pass — **abitest-tcc is now clean (24/24)**. (b) Implemented `rt_get_caller_pc` and `rt_getcontext` for Apple PPC (Tiger 10.4 `mcontext->ss.{srr0,r1}`). Backtraces from JIT-caught crashes now print real addresses (was `??`). libtest_mt's "producing some exceptions" stage now also passes — was failing because the signal-handler path couldn't walk the stack. Test1b now reports proper "RUNTIME ERROR: invalid memory access" with backtrace instead of silently dying. |

(Total dropped 122 → 118 in v0.2.6 because four LE-byte-order-
specific tests are properly classified as skipped on big-endian;
118 → 111 in v0.2.10/.11 because six bcheck-asserting tests are
properly skipped without a real bcheck.c port.)

* Bootstrap fixpoint (`tcc → tcc-self → tcc-self2 → tcc-self3` with
  `tcc-self2.o == tcc-self3.o`) holds at every release.
* `tcc -run hello.c` works (since v0.2.7).
* `tcc -o exe` produces working libSystem-using binaries.
* Multi-threaded atomics correct (since v0.2.8).
* tests2 at 100% (since v0.2.14).
* lua 5.4.7 builds + runs end-to-end (since v0.2.12).
* sqlite3 amalgamation: in-memory SELECT queries work
  (since v0.2.16). File-based sqlite_open_v2 still crashes
  in the file-open path — distinct bug, deferred.
* zlib 1.3.1 builds + full test suite passes (since v0.2.16).
* Compile-time benchmark vs gcc-4.0 (lua 5.4.7, 33 .c files):
  tcc 2 s, gcc -O0 17 s, gcc -Os 40 s on a 900 MHz iBook G3.

## Open work, prioritized

### Concrete remaining test failures (0)

**tests2 is at 111/111 (100%) since v0.2.14.** All previously-
failing tests have either been fixed or properly classified as
skipped (bcheck-asserting tests are skipped on PPC because we
have no-op stubs but no real bcheck.c port).

The bcheck.c port remains a multi-session lift if we ever want
the `-b` instrumented build to actually instrument. See "Larger
scope" below.

### Structural items

| | item | notes |
|---|---|---|
| ~~**#1**~~ | ~~Real lwarx/stwcx atomics~~ | ✅ Done in `ba7848b` (4-byte) + `defd38c` (1-byte and 2-byte via word-RMW with masking). Per-file Makefile rule routes `tcc/lib/atomic-ppc.S` through gcc-4.0 to dodge tcc's missing PPC inline-asm. 124_atomic_counter dropped from 6m23s → 2.4s (137x speedup). 8-byte ops still serialize through the mutex (PPC32 has no ldarx/stdcx). |
| ~~**#2**~~ | ~~2-element HFA struct-by-value on GPR overflow~~ | ✅ Done in `84ae52c`. Wasn't actually a struct-by-value bug — the failing 73_arm64 sub-cases isolated to printf's variadic FP arg passing when GPR shadow slots run past r10. The straddle (gslot==7) and fully-past (gslot>=8) cases didn't write the FP value to the outgoing param stack, so printf read garbage. Same fix flipped 70_floating_point_literals to passing (the "5-ULP error" was printf reading garbage stack words, not parse_number precision). |
| ~~**#3**~~ | ~~Mach-O `tcc -ar` driver~~ | ✅ Done in v0.2.24-g3 (commit `9a5aa8d`). `tcc_tool_ar()` grew a Mach-O code path that handles fat-binary slice selection, LC_SYMTAB walk, and 12-byte nlist iteration. `XAR=$(AR)` workaround removed from `tcc/lib/Makefile`. tcc is now self-sufficient on Tiger PPC — no system `ar` needed in the build pipeline. |
| ~~**#3.5**~~ | ~~Mach-O `-shared` dylib output~~ | ✅ Done in v0.2.25-g3 (session 045). New `macho_output_dylib()` in `ppc-macho.c`. Produces real Tiger PPC Mach-O dylibs that dyld loads via dlopen. PIC stubs survive whole-image slide. Multi-dylib link-time support (loading dylibs as link-time inputs to resolve `tcc -lz`-style references) deferred to a follow-up. |
| ~~**#4**~~ | ~~Mach-O archive alacarte loader~~ | ✅ Done in v0.2.30-g3 (session 046). `tcc_load_alacarte_macho` parses the BSD `__.SYMDEF SORTED` symbol table and pulls in only the members that resolve currently-undefined symbols. The existing SysV-style `/` symdef path also got a Mach-O codepath: it now sniffs each loaded member's magic and routes Mach-O `.o` files through `macho_load_object_file`, so tcc -ar-built archives (libtcc1.a) keep loading correctly. |
| ~~**#5**~~ | ~~`ppc-macho-stubs.c` cleanup~~ | ✅ Done in `00751c8`. Stubs file was dead since session 009 — `ppc-macho.c` had subsumed all its symbols. |
| ~~**#6**~~ | ~~De-duplicate UNDEF symbols~~ | ✅ Done in `dc7a05d`. The dupe came from emitting one UNDEF nlist entry per stubs[] element AND another per nlptrs[] element when both lists referenced the same elfsym (e.g. `_atexit` referenced as both call target via REL24 and data pointer via ADDR32 from crt1.o's static-init machinery). Refactored to build an elfsym→nlist map so stub_sym_idx and data_sym_idx share UNDEF entries. |
| ~~**#7**~~ | ~~Self-link diagnostics~~ | ✅ Done in v0.2.50-g3 (session 068). `macho_output_exe` runs four pre-write sanity checks (VM layout, required symbols, stub/slot wiring, section-presence) that turn the historical session-025 dyld errors into tcc-level diagnostics. Each check verified by injecting a deliberate break. Happy-path cost <1 ms per link. |
| ~~**#7.5**~~ | ~~OSO STAB emission for gdb-on-Tiger~~ | ✅ Done across v0.2.51-g3..v0.2.58-g3 (sessions 072..079). The full surface now: (a) **`tcc -gstabs ...` / `tcc -g ...` (post-v0.2.55)** produces a Mach-O whose `LC_SYMTAB` nlist carries the classic stabs+ chain Apple's Tiger `gdb 6.3` reads directly — file:line breakpoints, source listing, run/step/where with file:line per frame, `print <global>` / `print <param>` / `print <local>` (including stepped values), Apple-conventional `N_BNSYM` / `N_ENSYM` body-extent markers paired around each `N_FUN`, cross-`.o` debugging through the two-step compile-link flow real Makefiles use (Phase 1 in v0.2.51, item 1a in v0.2.52, item 1c in v0.2.53, item 1b's `.o` round-trip prerequisite in v0.2.54, item 1d's stabs-default in v0.2.55). (b) **`tcc -gdwarf-2 -c foo.c && tcc -gdwarf-2 foo.o -o foo && dsymutil foo -o foo.dSYM`** produces a `.dSYM` bundle that `dsymutil-on-Tiger` (cctools `dwarf_utilities-42`) consumes cleanly: tcc emits a synthesized `N_SO source / N_OSO objpath(mtime) / {N_BNSYM N_FUN(name) N_FUN(size) N_ENSYM} per function / N_SO ""` chain in `LC_SYMTAB` per loaded `.o`; tcc's per-function DWARF `AT_high_pc` now emits absolute end-VAs (strict DWARF-2 form) and `AT_frame_base` uses `DW_OP_reg31` (DWARF-2 op) rather than `DW_OP_call_frame_cfa` (DWARF-3 op Tiger's tools assert on); `dsymutil` follows the OSO records via mtime-verified realpath to each on-disk `.o`'s DWARF, consolidates the subprograms, and writes the .dSYM (Phase 2 item 1b in v0.2.56). (c) **`strip -S` + `.dSYM` distribution flow** — `tcc -gdwarf-2 ... -o exe; dsymutil exe; strip -S exe; gdb exe` now works as a single workflow with full source-line debug: v0.2.57 made `strip -S` accept tcc's `__DWARF`+`__LINKEDIT` layout and emitted `LC_UUID` so Tiger gdb 6.3 pairs the stripped exe with its companion `.dSYM` automatically (`__DWARF` originally laid before `__LINKEDIT` and page-padded; `__LINKEDIT` interior in gcc-4.0's `symtab → indirect → strtab` order; FNV-1a-derived deterministic 16-byte fingerprint over post-relocation bytes). v0.2.58 then closed the source-line gap: pre-v0.2.58 `dsymutil` produced a `.dSYM` whose `.debug_info` / `.debug_abbrev` / `.debug_line` / `.debug_str` dwarfdump reported as `< EMPTY >` (only `.debug_pubnames` populated), so `gdb list main` / `break <file>:<line>` failed on stripped exes. Root cause: the link-time keymgr-stub compile (the dyld init-order fix) was writing a `<string>` CU into the linker's `.debug_*` sections, baking a small `__DWARF` segment into the linked exe; `dsymutil` preserved that segment AND appended a second `__DWARF` with the consolidated content, but dwarfdump/gdb read the first (small) one and never the second. Fix: one-line `tccelf.c` change to suppress `s->do_debug` around the keymgr-stub `tcc_compile_string`, leaving the linker's `.debug_*` sections empty in a two-step build and making `macho_output_exe` drop the `__DWARF` segment entirely (gcc-4.0 parity); `dsymutil` then writes a single-`__DWARF` `.dSYM` that gdb / dwarfdump read cleanly. Full source-line debug of stripped exes via `.dSYM` is the final state (Phase 2 item 1b last piece, in v0.2.58). |

### Larger scope

| | item | notes |
|---|---|---|
| **#8** | ~~**DWARF debug info emission**~~ | ✅ Done in v0.2.37 (`-c` .o output), v0.2.38 (linked exe `__DWARF`), v0.2.39 (`__eh_frame` + per-prolog CFI; default DWARF-2). dwarfdump on tcc binaries now reads compile unit / line table / aranges / strings / per-FDE CFI directives (CFA position, LR save). Remaining gap is gdb-on-Tiger compatibility (#7.5 above). |
| **#9** | **AltiVec intrinsics** | (Was old #12.) None today; tcc emits scalar code for everything. Plausible but a large project. |
| **#10** | **Real-world program builds** | Ten programs build and run end-to-end on Tiger PPC, each with a runnable demo: Lua 5.4.7 (v0.2.12), zlib 1.3.1 (no separate demo), bzip2 1.0.8 (v0.2.18), cJSON 1.7.18 (v0.2.21), a BSD-sockets HTTP server (v0.2.21), sqlite3 with on-disk databases (v0.2.23 — full CREATE/INSERT/SELECT/ORDER BY round-trip; closes session 041's deferred Heisenbug stack: `prepare_v2` fixed in v0.2.16, CREATE TABLE → `SQLITE_CORRUPT` fixed by v0.2.22's addend work, file-open SEGV fixed by v0.2.23's stub-for-data-referenced-extern fix), sqlite3 against a tcc-built `libsqlite3.dylib` (v0.2.28), GNU sed 4.8 (v0.2.40), GNU gzip 1.11 (v0.2.41), Python 2.7.18 (v0.2.42 — ~150 .c files, ~600k LOC), Brian Kernighan's `awk` (one-true-awk, v0.2.66 — surfaced the `__builtin_fabs` / `__builtin_inf` implicit-int-return ABI bug in Tiger's `<math.h>` inline `isinf`/`isnan`/`isfinite` path), and libexpat 2.5.0 (session 090, builds on v0.2.66 — surfaced the `AC_CHECK_FUNCS` false-positive issue: tcc's PPC writer doesn't validate undefined externals against linked dylibs' export tables, so autoconf's link-and-see probes report every function as available; workaround is `CFLAGS=-Werror=implicit-function-declaration` which makes tcc fail conftests when they implicitly-declare a missing symbol). See [`demos/`](../demos/) for the per-program runners. No specific next-target gates the roadmap; the next one is whichever real-world program a future session tries that surfaces a new bug. Session 044's deferred extern-data sub-item — `int *p = &optind;`-style static-init refs to libSystem extern data — was closed by [v0.2.63-g3](sessions/086-extern-data-ref-2026-05-15/README.md), which emits classic Mach-O external relocations in `__LINKEDIT` so dyld binds the slot at load time. Session 086's residual `.o`-roundtrip-STT_FUNC-loss problem — sed's `dfa.c::prednames[]` table of ctype function pointers in `__TEXT,__const` SIGSEGV'd when sed's regex actually hit a character class — was closed by [v0.2.64-g3](sessions/087-stt-func-roundtrip-2026-05-15/README.md), which reconstructs `STT_FUNC` on undef syms at link time via REL24-target and .rodata-ADDR32-target heuristics so the existing v0.2.23 stub-allocation logic catches the function-pointer-in-const case. |
| **#11** | **bcheck.c port** | If we want `-b` instrumented builds to actually instrument. Multi-session lift. Codegen hooks in `ppc-gen.c` partially exist (`func_bound_offset` / `func_bound_ind` are unused statics); the no-op stubs in `lib-ppc.c` need replacing with a real port. |
| **#12** | **PPC undefined-external symbol validation** | NEW from session 090. `ppc-macho.c`'s EXE writer emits undefined external symbols into the Mach-O nlist table without verifying they're actually exported by any linked dylib. `tccmacho.c`'s `check_symbols` (used by x86_64/arm64 macOS) walks `symtab_section`, classifies each `SHN_UNDEF` symbol, and errors if it's not weak, not from `s1->dynsymtab_section`, and we're producing an executable — the PPC writer never got the equivalent. User-visible failures: autoconf's `AC_CHECK_FUNCS` reports every probed function as present (because link succeeds), then `dyld` fails at runtime with `Symbol not found: _funcname`. Found via expat (HAVE_ARC4RANDOM_BUF false-positive). Proper fix has three parts: (1) ensure libSystem's exports are loaded into `s1->dynsymtab_section` by default for PPC EXE output (currently only loaded when `-lSystem` is explicit) so the dynsymtab has the right contents to check against; (2) port `check_symbols`-shape logic into `ppc-macho.c`, walking `symtab_section` for `SHN_UNDEF + STB_GLOBAL` syms and calling `find_elf_sym(s1->dynsymtab_section, name)`, error if not found and not weak; (3) decide on an opt-out flag (`-undefined dynamic_lookup` is the gcc/ld convention) for the rare programs that legitimately rely on flat-namespace runtime lookup. Today's workaround for AC_CHECK_FUNCS users: `CFLAGS=-Werror=implicit-function-declaration` flips the implicit-declaration warning that tcc already emits into a hard error, breaking the conftest before the link step. |

## Out of scope (still)

- C++ support. TCC has none anywhere.
- Cross-compilation FROM ppc TO other targets. We only need
  ppc-darwin8 native.
- 64-bit PPC (G5 in 64-bit mode). 32-bit only; runs on G5 in 32-bit
  mode anyway.
- libtcc thread safety. Disabled at build time.
- Inline assembly support beyond the basic `__asm__` block (gating
  some of #1 above; if it ever lands, would unblock several
  follow-ups).

## Testing methodology

In rough order of cost vs. confidence:

1. **`scripts/run-tests2.sh`** — full TCC tests2 suite.
   `NORUN=true` (default) exercises `-o exe`; `RUN=1` exercises
   `tcc -run`. ~7 min wall under default since v0.2.8 (was ~3 min
   before real atomics; 124_atomic_counter takes 6+ min through
   the global mutex).

2. **`FIXPOINT=1 ./scripts/bootstrap-tcc-self.sh`** — bootstrap
   fixpoint. tcc → tcc-self → tcc-self2 → tcc-self3, verifying
   `tcc-self2.o == tcc-self3.o`. Catches any regression that
   affects tcc.c's own compilation.

3. **The pre-release demos** (`demos/s00*-*.c`,
   `demos/s025-self-link.sh`). Fast smoke for basic codegen + exec
   output after non-trivial changes.

4. **Differential testing against `gcc-4.0`** for new features.
   Compile the same C with both, compare output / exit code.
   Cheapest way to catch semantic divergence.

5. **Real-world program builds** (sqlite, lua). Smoke-tests the
   broader codegen surface that synthetic tests don't exercise.

6. **Upstream `make -k test` in `tcc/tests/`** (since session 042).
   Runs hello-exe / hello-run / libtest / libtest_mt / test3 /
   abitest-cc / abitest-tcc / vla_test-run / tests2-dir / pp-dir /
   memtest / dlltest / cross-test / btest / tccb. Passing as of
   v0.2.62-g3: version, hello-exe, hello-run, libtest,
   vla_test-run, pp-dir (24/24), tests2-dir (111/111), memtest,
   abitest-cc, abitest-tcc (steady-state since session 047),
   dlltest (since v0.2.25 / session 045 added Mach-O dylib
   output). Still failing on known-deferred items: libtest_mt and
   test3 (the JIT-context heisenbug from session 044 open item A1),
   btest / test1b / tccb (real `bcheck.c` port, #11), cross-test
   (needs `dispatch/dispatch.h`, out of scope on Tiger). See
   [session 042 README](sessions/042-upstream-tests-2026-05-05/README.md)
   for the original snapshot.

## Risk register

- **bcheck blocker**. Five test failures plus three `-run` regressions
  all gate on the same `bcheck.c` port. Multi-session lift. Until
  it lands the test count is stuck around 89-90%.

- **lwarx/stwcx blocker**. The pthread_mutex atomics are correct
  but the mutex-contention scaling is bad. A real implementation
  needs tcc to grow inline-asm OR a separate `.S` build path; that's
  a prerequisite, not a small thing.

- **Tiger's libSystem is old**. Functions added after 10.4 (e.g.
  several POSIX 2008 additions) aren't available. Programs using
  them fail to link with informative errors. Constraint to
  communicate, not really a risk.

- ~~**No DWARF / no debugger**~~. ✅ Fully retired. The classic-
  stabs flow shipped through v0.2.37/.38/.39 (DWARF in .o + linked
  exe + `__eh_frame` with per-prolog CFI; dwarfdump and lldb read
  it directly), v0.2.51 (Apple-format STAB chain in the nlist
  symtab; Tiger's bundled `gdb 6.3` sets file:line breakpoints,
  walks stack frames, lists source, prints globals), v0.2.52
  (`N_PSYM` / `N_LSYM` offsets translated to gdb's SP-relative
  convention so `print x` and `print local` read correct values,
  including stepped values and `bt` frame argument display), and
  v0.2.53 (Apple-conventional `N_BNSYM` / `N_ENSYM` body-extent
  markers around every `N_FUN`). The Phase 2 `.dSYM` flow then
  shipped through v0.2.56 (synthesized `N_SO / N_OSO / {N_BNSYM
  N_FUN N_ENSYM} / N_SO ""` chain per `.o`; DWARF fixes for
  `AT_high_pc` absolute end-VAs and `AT_frame_base` using
  DWARF-2's `DW_OP_reg31`), v0.2.57 (`strip -S` + `LC_UUID` so
  Tiger gdb 6.3 auto-pairs the stripped exe with its companion
  `.dSYM`), and v0.2.58 (suppressed the link-time keymgr-stub's
  in-line `__DWARF` segment so `dsymutil` writes a single
  `__DWARF` segment that gdb / dwarfdump read cleanly).
  `tcc -gdwarf-2 ... -o exe; dsymutil exe; strip -S exe; gdb exe`
  now works as a single workflow with full source-line debug.

- **Out-of-tree upstream divergence**. We modify tcc/ in place
  rather than maintaining a patch set. If we ever want to rebase
  onto a newer upstream, that's a session task — not a routine
  workflow.
